/**
 * Shared object-view helpers (see `object-view.ts` for the contracts).
 * Workers 2/3 extend this surface; nothing here renders domain state.
 */
export type { ObjectField, ObjectViewSection, ObjectReference, ObjectActivityEntry } from "./object-view";
