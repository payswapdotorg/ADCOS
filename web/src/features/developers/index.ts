/**
 * Developers feature (Worker 3 — plan Task 9: developer workspace).
 *
 * Application detail, credential state (masked; one-time reveal where the
 * backend supports it), capabilities with explanations for missing grants,
 * API version and recent activity. Placeholder barrel.
 */

export const DEVELOPERS_FEATURE = {
  area: "developers",
  route: "/developers",
  owner: "worker-3",
  clientSurface: ["applicationSelf"],
} as const;
