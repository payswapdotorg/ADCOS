/**
 * Home feature (Worker 2 — plan Task 4: Home + system health).
 *
 * The dashboard that answers, immediately and honestly: whether ADCOS is
 * healthy, what connectivity is managed, what is changing, what needs
 * attention — every number links to the underlying resource or evidence
 * chain — plus the deterministic fulfillment demonstration. No vanity
 * KPIs, no fake production values; backend values render verbatim.
 */

export { HomeView } from "./home-view";
