"use client";

/**
 * ConsoleSearchProviders — the Worker 3 search providers mounted on
 * every Worker 3 page (Developers, Evidence, Assurance, Settings).
 *
 * PRE-WORK STUB: subagent 3-c replaces this implementation with the
 * real providers (contracts, webhook endpoints, evidence records, API
 * operations — permission-aware, capability-gated, destructive
 * operations confirmed). The export shape is frozen so pages can mount
 * it before the implementation lands.
 */

export function ConsoleSearchProviders() {
  return null;
}
