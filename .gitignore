# OS/editor
.DS_Store
.idea/
.vscode/
*.swp

# Local secrets
.env
.env.*
!.env.example

# Build/cache
node_modules/
target/
dist/
/build/
coverage/
.cache/

# Local JS tooling debris (bun install side artifact; the repo tracks package-lock.json)
web/bun.lock

# Runtime state
*.db
*.sqlite
*.log


# Python tooling (added by WORK-001: spec checker bytecode caches)
__pycache__/
*.pyc

# Vercel CLI project link (station tooling — never commit)
.vercel/
.vercel
.env*

# vercel CLI local-build side artifacts (station debris — regenerated per build)
pyproject.toml
.python-version
