# ADCOS Work Item authorization record — WORK-044 historical authorization, now superseded by mainline-integrity halt.
# Original authorization issued by DEC-0063. Scope/provenance are preserved verbatim.
work_item: WORK-044
status: superseded
authorized: false
type: implementation
authorization_id: "WORK-044-CORE-001"
authorization_decision: DEC-0063
superseded_by: null
supersession_decision: DEC-0080
baseline_sha: bb29c11c8bba6c9db5b87f85b1d62faad0bf7825
review_basis: "Historical authorization issued by DEC-0063 after W053 acceptance. Its scope and provenance remain recorded for audit; DEC-0080 supersedes its authority because current mainline integrity is blocked."
dependencies:
  - WORK-051
  - WORK-053
authority_inputs:
  - "WORK-053 EconomicAllocation (accepted-merged by DEC-0063)"
  - "WORK-052 UsageLedger (accepted-merged by DEC-0061)"
  - "WORK-051 CommercialCore (accepted-merged by DEC-0059)"
  - "WORK-033 AgentClock seam"
authority_outputs:
  - "the provider-neutral payment/settlement adapter boundary only; never a custody, KYC/KYB, jurisdiction, usage, allocation, or connectivity authority"
scope:
  - "payment/"
  - "tools/payment_selftest.py"
  - "docs/WORK-044-evidence.md"
  - ".github/workflows/spec-check.yml"
acceptance_criteria:
  - "Idempotent payment intent creation/retrieval, authorization/capture/refund/reversal status mapping, and payout/transfer instruction emission from finalized allocations work through an abstract provider adapter without importing provider-specific semantics into the canonical ledger."
  - "Provider callbacks are external observations until reconciled against ADCOS state; callback replay/duplicate/out-of-order delivery is idempotent, append-only, and never mutates settled history."
  - "Provider success never creates usage or connectivity-delivery facts and never bypasses billable-final requirements; corrections are compensating records."
  - "Reconciliation identifies provider/ADCOS divergence deterministically without rewriting history."
  - "Provider-specific capabilities are explicit and versioned; a deterministic sandbox provider proves the full intent/capture/refund/reversal/payout flows."
  - "No regulated funds movement, custody, merchant-of-record, KYC/KYB, or jurisdiction policy is implemented."
  - "Strict import/boundary discipline is preserved."
out_of_scope:
  - "W045 and every later Work Item"
  - "implementation of any downstream Work Item"
  - "payment custody or movement of regulated funds"
  - "KYC/KYB onboarding"
  - "jurisdiction policy"
  - "marketplace discovery"
  - "SDK/client/runtime surfaces"
  - "live payment-account onboarding"
  - "identity/session/routing/path/transport/federation/policy authority"
  - "usage or allocation authority"
  - "wire-schema or frozen architecture changes"
  - "modifying spec/architect/ from the implementation PR"
acceptance_gate:
  status: "Superseded before implementation; reactivation requires a new Architect decision under the frozen roadmap."
  required_evidence:
    - "the original W044 deterministic payment battery and exact accepted delivery provenance if restored"
    - "new authorization after R0/R1 if any further implementation is required"
handoff: docs/WORK-044-handoff.md
handoff_required: true
