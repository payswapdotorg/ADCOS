# ADCOS implementation authorization record — R9 Future Access Technology
# (bounded R9 program authorization, the DEC-0120 governance-compression class —
# the DEC-0101/DEC-0114 precedent). Issued by DEC-0120; ONE active
# authorization for the whole M020-M024 tranche. Per-child acceptance
# decisions (DEC-0121+) close each child; this authorization closes with the
# R9 gate completion (the M024 convergence child accepted and the completion
# review recorded — the terminal roadmap gate; the program_exit evaluation
# rides with that acceptance). Children: M020 (Access Technology Capability
# Envelope — ACCEPTED under DEC-0121: the chain-child pointer advanced
# M020 -> M021), M021 (Wireline Access Adapters — ACCEPTED under DEC-0122:
# the chain-child pointer advanced M021 -> M022), M022 (Non-Terrestrial
# Access Adapters — ACCEPTED under DEC-0123: the chain-child pointer advanced
# M022 -> M024 along the chain), M023 (Future Technology Extension Drill —
# chain-independent, IN FLIGHT), M024 (Future Access Convergence — the
# current child, the convergence child; blocked until the chain-independent
# M023 lands).
work_item: R9
child_work_items:
  - M020
  - M021
  - M022
  - M023
  - M024
current_child_work_item: M024
status: active
authorized: true
type: implementation
authorization_id: "R9-CORE-001"
authorization_decision: DEC-0120
scope_amendment_chain: []
superseded_by: null
supersession_decision: null
acceptance_decision: null
acceptance_mode: null
accepted_delivery_sha: null
acceptance_merge_sha: null
baseline_sha: ea4bb64e0d32cfe93384ece71ec0003f447e357f
review_basis: "DEC-0120 activated R9 as a bounded program authorization from the DEC-0119 R8-completion head ea4bb64 (live main, push CI green run 34675282864). R8 is COMPLETE (DEC-0119: all five children M015-M019 accepted; the R8-CORE-001 program authorization closed); Architecture 1.1 is the sole normative forward architecture; the accepted R7 and R8 child authorities are the composition substrate consumed BY REFERENCE. The gate-specific contract is the R9 charter (spec/architect/work-items/R9-charter.md); the dependency overlay is spec/architect/dependency-overlays/R9.yaml."
dependencies:
  - R8
authority_inputs:
  - "spec/mission.md (permanent Mission Authority)"
  - "Architecture 1.1 FROZEN (spec/architecture.md, spec/architecture-lock.md, LOCK-101..LOCK-120, ACR-014/DEC-0100)"
  - "The accepted R7 delivery state (DEC-0102..DEC-0109/DEC-0113: contracts/, offers/, eligibility/, policy/, evidence/, assurance/, executionplans/, adapters/, replan/, usage/, commercial/, allocation/, payment/, sharenet/, roamlink/, comos/, developerapi/, federation/, identity/, upgrade/, scale/, client/ and their batteries)"
  - "The accepted R8 delivery state (DEC-0115..DEC-0119: resilience/, localfirst/, recovery/, credentials/ and their batteries — the handover/realization-state machinery the R9 envelopes compose)"
  - "spec/migration/classification-matrix.md (the migration policy for harvesting legacy material: the imt/transport/topology access-technology reservoir is source material only)"
  - "spec/architect/work-items/R9-charter.md (the gate contract: child scopes, acceptance criteria, the by-reference consumption rule, evidence/worker/migration policies)"
  - "spec/architect/dependency-overlays/R9.yaml (the gate overlay)"
authority_outputs:
  - "The M020-M024 future-access-technology tranche delivered and accepted per child from repository evidence"
  - "The access-technology envelope domain, the wireline/non-terrestrial/future reference adapter compositions, and the R9 convergence surface on the canonical 1.1 authority — access technologies added and replaced through the adapter boundary with the connectivity contract core unaltered"
scope:
  # M020 — Access Technology Capability Envelope (ACCEPTED under DEC-0121: PR #44
  #  reviewed head 035242de, merge c75a7c70 — the current-child pointer advanced
  #  to M021)
  - "accesstech/"
  - "docs/M020-evidence.md"
  # Shared battery surface: every current/future per-child battery and the
  # authorization-aware battery-scope consultation repairs. The drift guard's
  # CONTROL_FILES independently freeze the governance checkers (spec_check,
  # current_spec_check, fresh_session_check, tech_lead_guard,
  # architecture_drift_guard, spec_check_selftest, tools/README.md) against
  # implementation-PR modification, so this prefix cannot reach them.
  - "tools/"
  # M021 — Wireline Access Adapters (chain child: requires M020 — SATISFIED by
  # the DEC-0121 M020 acceptance; the CURRENT child)
  - "adapters/reference/wireline.py"
  - "docs/M021-evidence.md"
  # M022 — Non-Terrestrial Access Adapters (chain child: requires M020, M021)
  - "adapters/reference/satellite.py"
  - "docs/M022-evidence.md"
  # M023 — Future Technology Extension Drill (chain-independent: from the
  # accepted R8-complete state — the synthetic future-IMT/6G-class composition
  # through the accepted public extension surface)
  - "adapters/reference/futureimt.py"
  - "docs/M023-evidence.md"
  # M024 — Future Access Convergence (the convergence child — its acceptance
  # completes the R9 gate and evaluates the program_exit conditions;
  # accesstech/ is the M020-owned shared prefix)
  - "docs/M024-evidence.md"
authorization_rules:
  - "Implementation PRs must classify as implementation-only (tools/architecture_drift_guard.py) and be fully covered by this scope (tools/authorization_provenance.py, byte-identical authorization inheritance from main)."
  - "The baseline-range semantics of the bounded program authorization apply: the baseline ea4bb64 may precede the PR branch point only through control-plane-only commits or commits covered by this authorization's own scope."
  - "Per-child acceptance is mandatory (one durable decision per child: DEC-0121+); acceptance is the sole serialization point."
  - "The by-reference consumption rule (R9 charter) is an acceptance criterion: no accepted authority reimplemented, weakened, forked, or bypassed; the connectivity contract core is never altered (the gate's own objective constraint)."
  - "Scope prefixes outside the declared list require a lightweight charter scope-amendment decision recorded in the decisions registry."
  - "This authorization closes only with the R9 gate completion (the M024 acceptance and the completion review with the program_exit evaluation, the DEC-0109/DEC-0119 precedent)."
acceptance_criteria:
  - "Per child: the deterministic battery green on the exact reviewed head (x3 byte-identical, the standing determinism bar), the full existing battery suite green, the evidence doc (docs/M02x-evidence.md) verification matrix + lock-conformance mapping + disclosed deviations, implementation-only delta fully scope-covered, and the sole Architect's durable acceptance decision recording the exact reviewed head + merge SHA."
  - "M024 additionally requires the convergence review: every R9 child authority consumed by reference, the cross-technology interchange and replacement drills green, the converged-domain compatibility matrix and federation-scale convergence green, and the gate completion recorded with the program_exit evaluation (roadmap R9 COMPLETE, the authorization closed, SOFTWARE-class evaluation only — the open physical obligations recorded as open)."
