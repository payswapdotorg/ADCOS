# ADCOS Current State

**R8 ACTIVE (DEC-0114) — Architecture 1.1 is the sole normative forward architecture, FROZEN with ACR-014 ACCEPTED; M001 ACCEPTED (DEC-0100); R7 — Universal Connectivity Commerce COMPLETE (DEC-0109: all thirteen chain children M002-M014 accepted and merged); the bounded R8 program authorization R8-CORE-001 (DEC-0114, baseline `28b3150`) covers the R8 charter child scopes M015-M019; M015 — Execution Resilience Runtime is ACCEPTED (DEC-0115: head `95a65a5`, merge `d77a561`); M016 — Local-First and Offline Operation is ACCEPTED (DEC-0116: head `0d5cfb7`, merge `a0ebd019`); M017 — Disaster Recovery and State Reconciliation is ACCEPTED (DEC-0117: head `f016124`, merge `438acb66`); M018 — Credential and Key Lifecycle Operations is chain-independent and in flight (the DEC-0118 slot); M019 — Resilience Convergence and Scale Hardening is the current chain child work item — its acceptance is BLOCKED until M018 lands.**

## Repository

- Repository: `github.com/payswapdotorg/ADCOS`
- M001 delivery: PR #25 head `36bfd8e636feafb531ef551a2e15793b57cc2f00` (branch `m001-architecture-1.1-freeze`, branch point `725397ffd60e7d8f44c24c85c86037f43ff6c303`), Architect-accepted under `DEC-0100` and merged as `80292c24502200f84d11491ed12e9cec5e5baf11`
- R7 activation: `DEC-0101` (governance compression) from the reconciled baseline `1e5c55f9916ff964d3bfc0761d04a8d5fd02c41b` (DEC-0100 acceptance head `4168f73` plus the fresh-session reconciliation repair; both push CI green)
- M002 delivery: PR #26 head `0112943c99c7fd3215bf7c8d2b0ebb740a282132` (branch `m002-contract-core`, base `7d28d9d` containing the R7-CORE-001 baseline `1e5c55f`), Tech-Lead-accepted under `DEC-0102` and merged as `0ffdf4775112c0b71b71f47f3688a50347ab4371` (push CI green run 34386408652; PR-head CI green run 34384904922)
- M003 delivery: PR #27 head `eace14349cbfae06235712778c578fbda0d4cffb` (branch `m003-offer-exchange`, base `d022e06` the DEC-0102-accepted head with the provenance repair), Tech-Lead-accepted under `DEC-0103` and merged as `4090e032324fc4fa5fcc6ffe81a85c22eca331a3` (PR-head CI green run 34394500718; merge-push CI green run 34396825502; the offer battery is CI-wired at acceptance)
- M013 delivery: PR #28 head `69ef8dee1adcf8c8a6f5855c9056d0364a0fd7b9` (branch `m013-developer-api`, base `d022e06`; zero file overlap with the M003 delivery), Tech-Lead-accepted under `DEC-0113` and merged as `8f4d58a23966a3af5242f37bab293a114cf5085a` (PR-head CI green run 34395941523; merge-push CI run 34397464404)
- M014 delivery: PR #38 head `689035e3803e308f2bb89cd11bc373d897b77416` (branch `m014-federation`, base `8516d50` the live DEC-0108 governance head), Tech-Lead-accepted under `DEC-0109` and merged as `344cd64e8396c7e388e31a50635ddff16bb4ea14` (the convergence child completing the R7 gate)
- M015 delivery: PR #39 head `95a65a54076b1401ea8ca414faa49a383ee2eb5f` (branch `m015-resilience`, base `b0145f1` the live DEC-0114 R8-activation head; five append-only worker-authored commits delivered across two worker sessions under the continuation charter), Tech-Lead-accepted under `DEC-0115` and merged as `d77a56197b0450ee04c61b356389cf8800aac288` (CI run 34641812859 green on the exact head; merge-commit run 34643002311 green)
- M016 delivery: PR #40 head `0d5cfb742d14bf3f257008593f7b0a22d98cc971` (branch `m016-localfirst`, base `6b96438` the live DEC-0115 M015-acceptance head; two append-only worker-authored commits delivered in ONE worker session), Tech-Lead-accepted under `DEC-0116` and merged as `a0ebd019a03238d089f03f006e9364e5c03cde51` (CI run 34650933081 green on the exact head; merge-commit run 34661179904 green)
- M017 delivery: PR #41 reviewed head `f016124f45193defd6343ff73b5efbe2b157b5cd` (branch `m017-recovery`, base `bda91c3` the live DEC-0116 M016-acceptance head; four append-only worker-authored commits delivered through the site-side destruction cycle — three dispatched sessions, the third completed the branch; the PR tip at merge `b742bec` — one append-only docs-only evidence-record precision amendment beyond the reviewed head), Tech-Lead-accepted under `DEC-0117` and merged as `438acb66a1e783acd9f28b353d823e610b4e2ebe` (CI run 34665760560 green on the exact reviewed head, both jobs; the merged-tip run 34666042297 and the merge-commit run 34666341364 green)
- Architecture: Version `1.1` FROZEN (`spec/architecture.md`, LOCK-101..LOCK-120); Version 1.0 preserved verbatim at `spec/history/architecture-1.0.md`
- Protocol baseline: `1.0` frozen unless/until a successor is separately accepted
- Roadmap: `2.14` frozen / authoritative (advanced from 2.13 by DEC-0117: the M017 acceptance — the THIRD R8 chain-child acceptance; M019 the current chain child with M018 in flight; the full R7 acceptance chain precedes it on the roadmap timeline)

## Program authority

`spec/architect/roadmap.yaml` is the sole canonical program roadmap. R0, R1, R2, R3, R5, R6 and M001 — Architecture 1.1 Freeze — are complete (R6 Provider Onboarding & Federation completed under DEC-0097/WORK-057). R4 remains an independent physical-validation track under W040. **R7 — Universal Connectivity Commerce is ACTIVE** under DEC-0101: one bounded R7 program authorization (`R7-CORE-001`, `spec/architect/authorizations/R7.yaml`) with the R7 charter (`spec/architect/work-items/R7-charter.md`) as the gate-specific Work Item contract and `spec/architect/dependency-overlays/R7.yaml` as the gate overlay. The R7 charter declared the child work-item scopes M002-M014; ALL THIRTEEN children were accepted and closed (M002 DEC-0102, M003 DEC-0103, M013 DEC-0113, M009 DEC-0109, M010 DEC-0110, M011 DEC-0111, M012 DEC-0112, M004 DEC-0104, M005 DEC-0105, M006 DEC-0106, M007 DEC-0107, M008 DEC-0108, M014 DEC-0109) and the R7 gate COMPLETED under DEC-0109 (the R7-CORE-001 program authorization CLOSED). **R8 — Resilience, Mobility and Scale is ACTIVE under DEC-0114** as the same governance-compression activation: one bounded R8 program authorization (`R8-CORE-001`, `spec/architect/authorizations/R8.yaml`) with the R8 charter (`spec/architect/work-items/R8-charter.md`) as the gate-specific Work Item contract and `spec/architect/dependency-overlays/R8.yaml` as the gate overlay. The R8 charter declares the child work-item scopes M015-M019; the Tech Lead may decompose, dispatch (3x3/9/depth-2), integrate and deliver within them without further pre-implementation authorization ceremonies. **Per-child acceptance remains mandatory** (one durable decision per child, exact reviewed head + merge SHA; the next is DEC-0118 for M018 — in flight, chain-independent; M019's acceptance waits for the M018 landing).

## Execution authority

- `active_work_item: M019` — Resilience Convergence and Scale Hardening is the current chain child in implementation (its acceptance is BLOCKED until the chain-independent M018 — in flight — lands; parallel preparation is permitted but never falsely claimed as acceptance)
- `active_authorization: R8-CORE-001` — the sole active implementation authorization (program-level, DEC-0114; the R8 charter child scopes M015-M019); `R7-CORE-001` is closed by the DEC-0109 gate completion (status accepted, authorized false); `M001-CORE-001` is closed by the DEC-0100 acceptance; all W-item authorizations remain closed
- M019 scope (R8 charter): `resilience/` (the convergence module — the M015-owned shared prefix), `tools/scale_selftest.py` (the DISCLOSED EVOLUTION battery — the M014 precedent class, already CI-wired and running, no new existence guard needed), `docs/M019-evidence.md`, plus the shared battery surface (`tools/`)
- M017 — Disaster Recovery and State Reconciliation is ACCEPTED (DEC-0117; head `f016124`, merge `438acb66`; the `recovery/` domain — snapshot/recovery-point construction with content-derived identities (LOCK-106) and full provenance, restore verification byte-exact-or-explicitly-disclosed-and-reconciled typed divergence (never silently absorbed), recovery-time bounds as declared deterministic operation counts (no wall clock — LOCK-119), cross-plane reconciliation with the attribution gates failing closed before any convergence drive, recovery never fabricating history (forged/re-identified records fail closed), fail-closed unverifiable-snapshot rejection (rejected whole, never partially trusted), the M015 runtime journals and M016 offline journals consumed BY REFERENCE as the drill substrate; the 36/36 battery CI-wired and green at the accepted head; delivered through the site-side destruction cycle — three sessions, the third completed the append-only branch, zero disclosed implementation defect fixes)
- M018 — Credential and Key Lifecycle Operations is chain-independent and IN FLIGHT (the parallel Worker B under the Task 56 charter; accepted in any order — the DEC-0118 slot; its battery step CI-wired with a pre-delivery existence guard by the DEC-0117 governance commit)
- M016 — Local-First and Offline Operation is ACCEPTED (DEC-0116; head `0d5cfb7`, merge `a0ebd019`; the `localfirst/` domain — deterministic offline operation journals with idempotent position-independent content identities, partition-tolerant admission with fail-closed stale-authority rejection (never a silent allow past the declared freshness window), explicit reconnect/resynchronization with divergence detection and LOCK-111 declared recorded deterministic convergence, LOCK-108 across the offline boundary (all 12 constraint kinds x DROP/RELAX/REINTERPRET rejected at admission and re-verified at resynchronization), journaled degraded-mode operation, the M015 runtime consumed BY REFERENCE as the composition substrate; the 35/35 battery CI-wired and green at the accepted head; a clean single-session delivery with zero disclosed defect fixes)
- M015 — Execution Resilience Runtime is ACCEPTED (DEC-0115; head `95a65a5`, merge `d77a561`; the `resilience/` runtime domain — session-runtime lifecycle with the WORK-012 explicit-reconnect discipline mechanically enforced, mobility handover with LOCK-108 per-kind weakened-constraint rejection, multipath failover with LOCK-111 declared recorded tie-breaking over the accepted executionplans/replan kernels, the M008 realization-state vocabulary consumed additively; the 36/36 battery CI-wired and green at the accepted head; delivered across two worker sessions under the continuation charter with two disclosed in-scope `resilience/` defect fixes)
- The R8 charter consumption rule: every accepted R7 authority is consumed BY REFERENCE — import and compose, never reimplement, weaken, fork, or bypass; the legacy resilience reservoir (sessions/mobility/multipath/edge/appliance) is harvest material per the frozen migration classification matrix, never forward design authority
- Implementation PRs under R8-CORE-001 must classify as implementation-only (drift guard) and be fully covered by the declared scope (ARCH-08 provenance, byte-identical authorization inheritance from main; the program-era baseline-range semantics apply)
- Subsequent feature work derives from the accepted 1.1 architecture and the accepted R7 delivery state; Architecture 1.0 semantics are never a forward design source

## Historical accepted state

W044–W047 and W049 were restored under R0 with acceptance provenance preserved. W048 remains accepted-not-restored and MUST NOT be recreated, mocked, or substituted implicitly. W050–W057 are accepted and present according to the authoritative roadmap/ledger. W040 remains independently in-review and unaccepted; its physical evidence remains separate from software evidence. The legacy implementation (adapters, commercial core, developer API, capabilities, policy, federation, mobility, sessions, usage, telemetry, composition, …) is a **migration reservoir** classified by the frozen migration matrix (`spec/migration/classification-matrix.md`); it is harvest material for the M003-M014 child scopes, never forward design authority.

## R8 — Resilience, Mobility and Scale (current gate)

R8 is ACTIVE under DEC-0114 as a governance-compression activation (the DEC-0101 precedent): one bounded program authorization with child work-item scopes instead of per-item pre-implementation ceremonies. The R8 charter carries the architectural invariants (LOCK-101..LOCK-120), the by-reference consumption rule, the child scopes and acceptance criteria (M015-M019), the dependency model (M015 → M016 → M017 → M019; M018 chain-independent from the accepted R7 state; M019 converging), the evidence policy (SOFTWARE-class deterministic batteries per child; physical obligations untouched), the migration policy (harvest per the classification matrix; no history rewriting; the legacy resilience reservoir is source material only), the 3-worker concurrency policy, and the per-child acceptance rules. **M015 — Execution Resilience Runtime is DELIVERED AND ACCEPTED (DEC-0115)**: the `resilience/` runtime domain — session-runtime lifecycle with explicit reconnect transitions (the WORK-012 discipline functionally enforced: every route change an explicit recorded reconnect event naming old AND new references, never a silent replacement), the deterministic replayable journal, mobility handover and multipath failover with LOCK-108 per-kind constraint-preservation and LOCK-111 deterministic tie-breaking over the accepted `executionplans/`/`replan/` kernels, the M008 realization-state vocabulary extended without redefining it, execution artifacts as opaque references (LOCK-117) — with `tools/resilience_selftest.py` (the 36/36 battery, CI-wired and green at the accepted head) and `docs/M015-evidence.md`; delivered across two worker sessions on the append-only branch under the continuation charter, with two disclosed in-scope `resilience/` defect fixes (each its own append-only commit). **M016 — Local-First and Offline Operation is DELIVERED AND ACCEPTED (DEC-0116)**: the `localfirst/` offline-operation domain — deterministic offline operation journals (ordered, idempotent, replayable — the position-independent content identity the idempotency key, LOCK-106), partition-tolerant fail-closed admission (a local state past its declared freshness bound never silently authorizes — the fold mechanically rejects the forged silent-allow shape), explicit reconnect/resynchronization with divergence detection and deterministic declared recorded convergence (LOCK-111 — both rule directions, never dropped, never duplicated), LOCK-108 across the offline boundary (all 12 constraint kinds x DROP/RELAX/REINTERPRET rejected with typed reasons at admission and re-verified at resynchronization — the wire-forged journaled admitted record rejected), journaled degraded-mode operation, the M015 runtime consumed BY REFERENCE as the composition substrate (the RuntimeStore lifecycle driven by reference for every offline transition) — with `tools/localfirst_selftest.py` (the 35/35 battery, CI-wired and green at the accepted head) and `docs/M016-evidence.md`; delivered in ONE worker session on the append-only branch — a clean single-session delivery with zero disclosed defect fixes. **M017 — Disaster Recovery and State Reconciliation is DELIVERED AND ACCEPTED (DEC-0117)**: the `recovery/` DR domain — snapshot/recovery-point construction with content-derived identities (LOCK-106) and full provenance (LOCK-118), restore verification byte-exact-or-explicitly-disclosed-and-reconciled typed divergence (the records beyond the recovery point LOST with the failure, disclosed one typed record each cited by their original content-derived identities and reconciled onto the point — never silently absorbed, never restored), recovery-time bounds as declared deterministic operation counts (LOCK-119 — no wall clock, AST-audited), cross-plane reconciliation with the attribution gates failing closed BEFORE any convergence drive (the accepted M016 close_partition/resynchronization driven BY REFERENCE with its typed divergence records preserved verbatim), recovery never fabricating history (every recovered record preserves its original content-derived identity, journal position and canonical bytes; forged/re-identified records fail closed), fail-closed unverifiable-snapshot rejection (rejected whole, never partially trusted) — with `tools/recovery_selftest.py` (the 36/36 battery, CI-wired and green at the accepted head) and `docs/M017-evidence.md`; delivered through the site-side destruction cycle — three dispatched sessions, the third completed the append-only branch while its page render stayed wedged the entire turn, zero disclosed implementation defect fixes. **M018 — Credential and Key Lifecycle Operations** is chain-independent and IN FLIGHT (branches from the accepted M014 identity/federation surfaces): the `credentials/` operational lifecycle domain (rotation drills with zero coverage gaps, revocation propagation rounds, emergency revocation, inventory verification, LOCK-119 fragment-assembled fixtures; its battery step CI-wired with a pre-delivery existence guard by the DEC-0117 governance commit). **M019 — Resilience Convergence and Scale Hardening** is the CURRENT chain child — the convergence child whose acceptance is BLOCKED until the chain-independent M018 lands (parallel preparation is permitted but never falsely claimed as acceptance); it converges M015+M016+M017+M018 last: the end-to-end fault-injection drill (fault → detect → replan/failover → recover → reconcile with every hard constraint preserved), the converged-domain compatibility matrix extended over the R8 domains, and the R8 gate completion review. R9 — Future Access Technology is the next gate after R8 (access-technology additions stay behind the adapter boundary — out of scope for this tranche).

## R7 — Universal Connectivity Commerce (completed gate)

R7 is ACTIVE under DEC-0101 as a governance-compression activation: one bounded program authorization with child work-item scopes instead of thirteen per-item pre-implementation ceremonies. The R7 charter carries the architectural invariants (LOCK-101..LOCK-120), the child scopes and acceptance criteria (M002-M014), the dependency model (M002 → M003 → M004 → M005 → M006 → M007 → M008; M009 from M002; M013 independent; M010/M011/M012 from M013; M014 converging), the evidence policy (SOFTWARE-class deterministic batteries per child; physical obligations untouched), the migration policy (harvest per the classification matrix; no history rewriting), the 3-worker concurrency policy, and the per-child acceptance rules. **M002 — Connectivity Contract Core is DELIVERED AND ACCEPTED (DEC-0102)**: the canonical `ConnectivityContract` domain — live under `contracts/` with its 54/54 battery. **M003 — Offers and Provider Capability Exchange is DELIVERED AND ACCEPTED (DEC-0103)**: the provider-domain offer model (`offers/` with LOCK-118 provenance, explicit bounded commitments, the LOCK-105 no-topology catalog exchange, LOCK-113 integer pricing reference shapes) plus the capabilities/discovery/marketplace harvest per the frozen migration matrix, with the 49/49 offer battery CI-wired at acceptance. **M013 — Developer Connectivity API is DELIVERED AND ACCEPTED (DEC-0113)**: the `developerapi/` surface harvested onto the canonical 1.1 authority (LOCK-114 opaque typed references; the gateway re-bound to the accepted contracts domain; schema 2.0 with the honest 1.x/0.8 retirement) with its 56/56 battery — the first two-worker parallel delivery round of the charter's 3-worker pipeline. **M009 — Usage and Commercial Reconciliation is DELIVERED AND ACCEPTED (DEC-0109)**: `usage/`, `commercial/`, `allocation/`, `payment/` harvested and re-bound to the canonical `contracts/` domain (LOCK-113 — canonical contract citations, never a second contract model), the DEC-0099 payment 44/44 and eligibility 46/46 honest re-baselines landed (CI guards converted to direct runs by the acceptance), battery evolutions usage 53/53, commercial 41/41, allocation 62/62 — a chain child that does not move the current-child pointer. **M010 — Vertical Proof — ShareNet is DELIVERED AND ACCEPTED (DEC-0110)**: the `sharenet/` harness proving the 8-step frozen flow on the accepted M002/M003/M013 authorities with disclosed deterministic seams for the pending children, LOCK-108 failover constraint preservation and the LOCK-120 external-application boundary verified, the 33/33 battery CI-wired at acceptance — a chain-independent vertical proof. **M011 — Vertical Proof — RoamLink is DELIVERED AND ACCEPTED (DEC-0111)**: the `roamlink/` harness proving the 6-step frozen flow on the accepted M002/M003/M009/M013 authorities with disclosed deterministic seams for the pending children — `cohort.py` the RoamLink application boundary over the M013 developer API SDK only (LOCK-120), opaque technology-neutral requirements (LOCK-102), provider-change absorption preserving hard constraints (LOCK-108), the 56/56 battery CI-wired at acceptance, and one disclosed sharenet-battery scope-consultation repair ratified — a chain-independent vertical proof. **M012 — Vertical Proof — COMOS is DELIVERED AND ACCEPTED (DEC-0112)**: the `comos/` harness proving the 6-step frozen flow on the accepted M002/M003/M009/M013 authorities with disclosed deterministic seams for the pending children — the COMOS external-application boundary (LOCK-120: COMOS-owned identity/communication-bundles/channel-semantics/delivery-semantics never implemented), LOCK-119 self-audited determinism, the 33/33 battery CI-wired at acceptance, zero disclosed repairs (the 7-file delta exactly the charter boundary) — a chain-independent vertical proof completing the M010/M011/M012 tranche. **M004 — Eligibility and Policy is DELIVERED AND ACCEPTED (DEC-0104)**: the deterministic constraint-decision and eligibility evaluation domains (`eligibility/` two modules — LOCK-101 typed reference facts with real contracts composed at the battery seam; LOCK-110 zero provider-SDK leakage; LOCK-119 fail-closed determinism), the policy battery evolved 74/74 → 103/103 (disclosed). **M005 — Evidence and Assurance is DELIVERED AND ACCEPTED (DEC-0105)**: the typed evidence-record domain (`evidence/` — LOCK-106: claim/observation/commitment/attestation distinct typed records; deterministic content-derived IDs; immutable append-only) and the closed-loop assurance evaluation (`assurance/` — LOCK-107: the FROZEN §9 state vocabulary exactly; deterministic (contract, evidence-set) evaluation; injected-instant stale detection), with the telemetry harvest disclosed via the explicit translation seam (RETAIN classification; the telemetry public API intact — battery evolved 34/34) and the 97/97 battery CI-wired at acceptance. **M006 — Execution Plan is DELIVERED AND ACCEPTED (DEC-0106)**: `executionplans/` five modules (LOCK-109 — the canonical execution-plan translation, the bridge from the `ConnectivityContract` to capability-oriented provider mechanisms; typed execution sequence; deterministic fail-closed) with the composition/ harvest disclosed one-way (the WORK-054 patterns refactored into `executionplans/conformance.py`; composition/ gains disclosure-only notes) and the 36/36 battery CI-wired at acceptance. **M007 — Provider/Standard Adapters is DELIVERED AND ACCEPTED (DEC-0107)**: `adapters/capability.py` + the `adapters/reference/` surface (LOCK-112 — reference adapters around existing standard/provider mechanisms behind the capability-oriented execution boundary; LOCK-110 — provider-SDK isolation, the core imports no adapter implementation and branches on no technology name; the WORK-016 compatibility seam with the explicit disclosed translation) with the battery expanded 56 → 70 cases (the CI step already wired and green at the merged head), one disclosed payment-battery frozen-family repair ratified (the DEC-0111 precedent). **M008 — Replan and Failover is DELIVERED AND ACCEPTED (DEC-0108)**: `replan/` eight modules (LOCK-108 — the constraint-preservation core: verbatim constraint-set equality both directions with typed weakened-constraint rejection and the pure no-silent-downgrade decision gate; LOCK-111 — deterministic declared recorded tie-breaking; the M008-owned realization-state vocabulary `REALIZING/DEGRADED/FAILED/SUPERSEDED` with the identity-preserving transition kernel; the explicit bridge into `contracts/` through its own frozen command vocabulary) with the `sessions/`/`mobility/`/`multipath/` harvest disclosed docstring-only (one-way seam — the functional harvest lives in `replan/execution_state.py`; the WORK-012 explicit-reconnect discipline mechanically enforced) and the 38/38 battery CI-wired at acceptance (cross-PYTHONHASHSEED determinism verified). **M014 — Production Federation is DELIVERED AND ACCEPTED (DEC-0109)**: the five convergence surfaces (`federation/`, `identity/`, `upgrade/`, `scale/`, `client/` convergence.py — every accepted child authority consumed BY REFERENCE; LOCK-117 production hardening: rate limits, revocation, credential lifecycle, compatibility surfaces) with the client battery DEC-0099 re-baseline (24/24; W048 never restored) and the scale battery evolution (45/45). **R7 — Universal Connectivity Commerce is COMPLETE (DEC-0109): all thirteen children M002–M014 accepted, the R7-CORE-001 program authorization CLOSED. R8 — Resilience, Mobility and Scale is the next gate, UNLOCKED and NOT ACTIVATED — no implementation authorization is active (the post-M001-acceptance halt pattern).**

## Architecture 1.1 transition

The transition is complete. Architecture 1.1 (`spec/architecture.md`, LOCK-101..LOCK-120) is the sole normative forward architecture, permanently; Architecture 1.0 is preserved historical evidence at `spec/history/`, used only to understand and migrate existing implementation. All new semantics and forward Work Items trace to 1.1. The authoritative transition record is ACR-014 (ACCEPTED) with the DEC-0100 acceptance chain.

## Evidence obligations

The following external evidence obligations remain open and must remain visible in every current-state projection until an Architect decision closes them:

- `EVID-002` — real SDR hardware topology for WORK-020.
- `EVID-003` — Raspberry Pi-class physical hardware for WORK-034.
- `EVID-004` — physical Android handset-backed second-path rebind/handover for WORK-035.
- `EVID-005` — physical Network-in-a-Box deployment at a real isolated site for WORK-036.
- `EVID-006` — real 5G interoperability lab for WORK-037.
- `EVID-007` — real users and physical devices for the WORK-040 pilot.
- `EVID-008` — a real 5G access path for the WORK-040 pilot.

`EVID-001` is closed and remains the only currently closed physical obligation. R7 software evidence never converts to PHYSICAL PASS.

## Source of truth

This file is a current-state projection only. Lifecycle history is governed by `spec/architect/execution-ledger.yaml`; architecture-transition authority by accepted ACRs; program order by `spec/architect/roadmap.yaml`; the R7 gate contract by `spec/architect/work-items/R7-charter.md`; permission by `spec/architect/authorizations/`. No conversation context is required or authoritative.
