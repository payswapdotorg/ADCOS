# DEC-0117 — M017 acceptance: Disaster Recovery and State Reconciliation accepted; the current-child pointer advances M017 → M019
# Sole-Architect governance decision (per-child acceptance, the DEC-0114 R8 serialization point; the THIRD R8 chain-child
# acceptance — the chain M015 → M016 → M017 → M019 with M018 chain-independent and in flight).
decision_id: DEC-0117
date: "2026-09-12"
type: acceptance
status: ACCEPTED
owner: "Architect"
work_item: M017
authorization: R8-CORE-001

review_basis: "Tech Lead review of the exact M017 delivery head f016124f45193defd6343ff73b5efbe2b157b5cd (PR #41, branch m017-recovery, base bda91c3 — the live DEC-0116 M016-acceptance head, correctly rooted at dispatch; four append-only worker-authored commits, no rebase/force-push/amending, delivered through the site-side destruction cycle by the third dispatched session — see delivery_path_disclosure; the PR tip at merge b742bec sits ONE append-only docs-only commit beyond the reviewed head — the worker's evidence-record precision amendment to docs/M017-evidence.md, +6/-3, no code, no battery, no scope change, CI run 34666042297 green on it, both jobs): the recovery battery (tools/recovery_selftest.py, 36 deterministic offline seeded cases covering the charter's (a)-(h) acceptance matrix) 36/36 PASS run 3x consecutively with byte-identical logs at the exact head (2026-09-12T01:51-01:56Z); the full workflow suite executed in exact CI order at the head — 65/65 green (the M017 recovery step activated by this delivery) with exactly the one documented non-zero exit (the legacy spec_check.py compatibility audit baseline, continue-on-error: true); CI green end-to-end on the exact delivery head (run 34665760560, both jobs — the spec-check job and the platform-capability job), on the merged PR tip (run 34666042297, both jobs) and on the merge commit (run 34666341364); scope verified 8 files, +6942/-0 — recovery/ x6 NEW (errors.py 98, model.py 1824, snapshot.py 410, verify.py 581, drill.py 576, __init__.py 167 lines — the disaster-recovery and state-reconciliation domain composing the accepted authorities BY REFERENCE: contracts/, resilience/, localfirst/, evidence/ consumed, never reimplemented/weakened/forked/bypassed; the legacy reservoir un-imported and unmodified source material), tools/recovery_selftest.py NEW (2970 lines), docs/M017-evidence.md NEW (316-line SOFTWARE-class matrix at the reviewed head) — every delta file covered by the R8-CORE-001 M017 scope entries; the governance gates 5/5 (current_spec_check, tech_lead_guard, architecture_drift_guard implementation-only 8 files 0 control-plane, fresh_session_check with --actual-main-sha bda91c3, authorization_provenance); the structural invariants verified (snapshot/recovery-point construction with CONTENT-DERIVED identities — the LOCK-106 class: identity from content, never position or time — and full provenance, LOCK-118, a provenance-missing snapshot rejected whole; restore verification byte-exact-or-explicitly-disclosed-and-reconciled typed divergence — the records the pre-failure plane carried beyond the recovery point are LOST with the failure, disclosed one typed record each cited by their ORIGINAL content-derived identities and reconciled onto the recovery point as the authoritative base, never silently absorbed, never restored — restoring them would fabricate history; recovery-time bounds as DECLARED deterministic operation counts — LOCK-119, no wall clock, no sleeps, no timing measurements anywhere in recovery/, AST-audited, an over-budget result fails closed at construction and a counter that exceeds the declared bound fails closed at the increment point; cross-plane reconciliation — the contracts/journal/evidence planes converge with typed divergence records, the attribution gates failing closed BEFORE any convergence drive (a journal that does not ride the owning contract is unreconcilable — LOCK-101/LOCK-117), the accepted M016 close_partition/resynchronization driven BY REFERENCE with its typed divergence records preserved VERBATIM, an unresolved composition link or a missing evidence record disclosed as a typed divergence record, never silently dropped; recovery never fabricates history — LOCK-106 — every recovered record preserves its original content-derived identity, its journal position AND its canonical bytes, a forged or re-identified record failing closed BOTH as a restored record (recovery-history-fabricated) and as a snapshot payload (recovery-snapshot-unverifiable, rejected whole); fail-closed recovery — an unverifiable snapshot REJECTED WHOLE, never partially trusted, never best-effort loaded, five rejection classes each failing at the load, the verification and the restore; the M015 runtime journals AND the M016 offline journals consumed BY REFERENCE as the drill substrate — the snapshots through the ACCEPTED read surfaces, the restores through the ACCEPTED construction-is-recovery constructors (RuntimeStore.from_events / OfflineJournal.from_records, the accepted folds the sole writers of plane state), the convergence through the ACCEPTED close; one-way imports — no accepted authority imports recovery/); ZERO disclosed implementation defect fixes (a clean delivery once the surviving session ran — no delivery_repairs_disclosed section; the one post-verification commit b742bec is a docs-only disclosure-precision amendment, not an implementation repair); the accepted sibling batteries at their accepted counts on this head (resilience 36/36 and localfirst 35/35 among them). The review is durably recorded in the PR #41 review timeline (review comment 5184754533, COMMENTED state, posted 2026-09-12T01:57:56Z); GitHub self-approval was unavailable (the single operator account owns both the PR and the review), so this decision is the authoritative acceptance record per repo convention."

decision: ACCEPTED

delivery:
  pr: 41
  branch: "m017-recovery"
  baseline_sha: bda91c39fb80c89dc3df1eaf1864f6560558a16b
  # pr_head is the PR tip AT MERGE (the merge commit's second parent); it sits
  # one append-only docs-only commit beyond reviewed_sha (the exact verification
  # head where every battery/gate/suite number in this record was taken):
  # b742bec — the worker's evidence-record precision amendment to
  # docs/M017-evidence.md (+6/-3, no code, no battery, no scope change),
  # pushed during the verification window and green on its own CI run
  # 34666042297 (both jobs).
  pr_head: b742becaaf1ae8b682d7c13da4d01dc4fec38a49
  reviewed_sha: f016124f45193defd6343ff73b5efbe2b157b5cd
  merge_sha: 438acb66a1e783acd9f28b353d823e610b4e2ebe
  merged_at: "2026-09-12T01:58:03Z"
  ci_run: 34665760560
  merge_ci_run: 34666341364
  worker_session: "m017-recovery (the full M017 charter delivered through the site-side destruction cycle — THREE dispatched sessions, the third (76d594e5) delivered the COMPLETE work: the recovery/ package at cf79c9c, the battery + the evidence record at 8786cce, the cross-plane attribution gate at f016124, the PR opened 2026-09-12T01:45:36Z, and the docs-only evidence-record precision amendment at b742bec; no rebase, no force-push, no amending — see delivery_path_disclosure)"

# DESTRUCTION-CYCLE DISCLOSURE (modeled on DEC-0115's continuation_disclosure,
# but this is a destruction-cycle disclosure, not a continuation): the delivery
# spans THREE site-side sessions; the capacity peak destroyed the first two and
# the third delivered the complete work with zero disclosed implementation
# defect fixes — no delivery_repairs_disclosed section (the DEC-0116 clean-
# delivery convention holds once the surviving session ran).
delivery_path_disclosure:
  session_1: "29910666 (dispatched 2026-09-12T00:52Z under the Task 55 M017 charter) — DESTROYED by the site while early-generating at 00:54 (the tab rolled home to the site landing page while the transcript was still forming); the queue_watch assault loop detected the destruction, voided the session per the boot rules and re-dispatched immediately"
  session_2: "4d9cf628 (the queue_watch re-dispatch at 00:54) — DESTROYED mid-queue before any content landed (00:56-00:58Z); its conversation UUID was re-instantiated by the site as 76d594e5 when capacity freed — no re-dispatch churn, no content lost"
  session_3: "76d594e5 — delivered the COMPLETE work while its page render stayed WEDGED the entire turn (trap 19b live evidence: the transcript page froze at ~17k chars while the worker pushed three commits — cf79c9c 2026-09-12T01:19:10Z, 8786cce 01:31:13Z, f016124 01:38:52Z — and opened the PR at 01:45:36Z, then the docs-only precision amendment b742bec 01:51:16Z; the queue_watch's stuck-clock started 01:26:53Z but NEVER assaulted because the BRANCH flag demonstrated progress — a pushed worker is never assaulted, the Task 54 hardening)"
  destruction_context: "the capacity-peak destruction cycle (the GLM-5.3-era window) destroyed the first TWO dispatched sessions during the M017 delivery window (00:52-00:58Z); the append-only branch carried nothing at the first destruction (the re-dispatch started clean), the second destruction was mid-queue (the site re-instantiated the UUID — no branch state existed yet, so nothing needed the continuation-charter recovery M015 had used), and the third session delivered the full charter end to end under the wedge; nothing was rebased, force-pushed, or amended — the destruction-cycle disclosure in deliberate contrast with BOTH M015's two-session continuation (content survived on the branch across the destruction window) and M016's clean single-session delivery"

acceptance_effects:
  current_child_advances: "M017 -> M019 (the THIRD R8 chain-child acceptance; the chain is M015 -> M016 -> M017 -> M019 with M018 chain-independent; M019 — Resilience Convergence and Scale Hardening is the next CHAIN child per the R8 overlay dependency_model; HOWEVER M019's acceptance is BLOCKED until M018 is accepted — its declared dependencies are M015+M016+M017+M018 — so the roadmap records: M018 pending (in flight, chain-independent, accepted in any order — the DEC-0118 slot), M019 the current chain child whose acceptance waits for the M018 landing; parallel preparation is permitted but never falsely claimed as acceptance — the charter's own rule; the R8-CORE-001 program authorization STAYS ACTIVE — closes only with the R8 gate completion at the M019 acceptance, the DEC-0088/DEC-0108 mid-chain precedent)"
  roadmap_version: "2.13 -> 2.14 (M017 COMPLETE; the R8 gate stays ACTIVE with M019 the current chain child and M018 in flight)"
  authorization_state: "R8-CORE-001 stays ACTIVE (the bounded R8 program authorization; the current-child binding advances M017 -> M019 in R8.yaml with the M018-in-flight dependency note in the accepted-children record)"
  ci_wiring: "The M018 credential battery is CI-wired by this acceptance with a pre-delivery existence guard (a visible skip until the authorized M018 delivery lands — the M002/contract precedent, exactly the M016/M017 wiring pattern; the guard message references the authorized M018 delivery R8-CORE-001 DEC-0117); the M017 recovery battery step (wired by DEC-0116, activated by this delivery merge) runs green 36/36 at the accepted head; NOTE on M019: NO new existence guard is needed — its battery is a DISCLOSED EVOLUTION of the existing tools/scale_selftest.py (already CI-wired and running), the M014 precedent class"
  ledger: "M017 accepted-merged entry appended + LEDGER-RECON-031 (snapshot baseline bda91c3 -> the M017 acceptance merge 438acb66 — through the DEC-0114 activation b0145f1, the PR #39 delivery merge d77a561, the DEC-0115 acceptance 6b96438, the PR #40 delivery merge a0ebd019 and the DEC-0116 acceptance bda91c3, the DEC-0109/DEC-0114 RECON semantics; this governance commit sits beyond the new pin per the standing reconciliation convention)"

verification_summary:
  battery: "tools/recovery_selftest.py 36/36 x3 consecutive runs with byte-identical logs at the exact delivery head f016124 (2026-09-12T01:51-01:56Z); cross-PYTHONHASHSEED determinism verified in-battery (case_08)"
  full_suite: "the full workflow suite in exact CI order at the head — 65/65 green (the M017 recovery step activated by this delivery) with exactly the one documented non-zero exit: the legacy spec_check.py compatibility audit baseline (continue-on-error: true); every battery at its accepted count (resilience 36/36, localfirst 35/35, contract 54/54, offer 49/49, developerapi 56/56, usage 53/53, commercial 41/41, sharenet 33/33, roamlink 56/56, comos 33/33, assurance 97/97, executionplan 36/36, adapter 70/70, replan 38/38, payment 44/44, eligibility 46/46, policy 103/103, client 24/24, scale 45/45, conformance 63/63)"
  gates: "current_spec_check PASS; tech_lead_guard PASS; architecture_drift_guard PASS (implementation-only, 8 files, 0 control-plane); fresh_session_check PASS (--actual-main-sha bda91c3 at review time); authorization_provenance PASS (8/8 covered by R8-CORE-001: recovery/, tools/recovery_selftest.py, docs/M017-evidence.md)"
  ci: "run 34665760560 SUCCESS on the exact delivery head f016124 (both jobs — the spec-check job and the platform-capability job); run 34666042297 SUCCESS on the merged PR tip b742bec (both jobs); the merge-commit run 34666341364 SUCCESS on 438acb66 (in flight at package-assembly time; completed green before the acceptance commit was finalized — the Tech Lead re-verified before push); the merge is a clean merge-commit of a branch rooted at the main head bda91c3"
  scope: "8 files, +6942/-0 at the reviewed head: recovery/ x6 NEW (errors.py 98, model.py 1824, snapshot.py 410, verify.py 581, drill.py 576, __init__.py 167), tools/recovery_selftest.py NEW (2970 lines, 36 cases), docs/M017-evidence.md NEW (316 lines at the reviewed head; 319 at the merged tip with the worker's docs-only precision amendment, +6/-3) — the charter boundary honored exactly; ZERO disclosed implementation defect fixes (a clean delivery once the surviving session ran — no delivery_repairs_disclosed section, nothing to disclose)"

physical_obligations: "EVID-002..EVID-008 remain OPEN and untouched; SOFTWARE-class deterministic evaluation only; R4/W040 remains independently in-review and unaccepted"

standing_rules:
  w048: "accepted-not-restored; MUST NOT be restored implicitly or explicitly"
  architecture: "Architecture 1.1 remains the sole forward design source; the R8 charter by-reference consumption rule is an acceptance criterion at every remaining R8 child (no accepted authority reimplemented, weakened, forked, or bypassed)"
  history: "No work-item history is rewritten; the M017 ledger entry is appended, never converted; the M008 docstring-only disclosures in sessions/, mobility/, multipath/ stay exactly as accepted"
