# DEC-0123 — M022 acceptance: Non-Terrestrial Access Adapters accepted (the THIRD R9 chain child);
# the current-child pointer ADVANCES M022 -> M024 along the chain (M023 chain-independent, never
# holds the pointer — the DEC-0117/DEC-0118 precedent class)
# Sole-Architect governance decision (per-child acceptance, the DEC-0120 R9 serialization point;
# the DEC-0115/DEC-0117/DEC-0121/DEC-0122 chain-child precedent class: the chain is M020 -> M021 ->
# M022 -> M024 with M023 chain-independent, and the current-child pointer follows the CHAIN).
decision_id: DEC-0123
date: "2026-09-13"
type: acceptance
status: ACCEPTED
owner: "Architect"
work_item: M022
authorization: R9-CORE-001

review_basis: "Tech Lead review of the exact M022 delivery head 16eb2748892b5db40e26fbe5db7e612dd1f4b0d0 (PR #46, branch m022-satellite rooted at the live DEC-0122 M021-acceptance head 9ee861126b2046ea8fb1ea698ba8b0554cac8452 — the live-main rule, no root-behind-main reconciliation; four append-only commits: ed7406b the reference compositions, 2a3bacd the declarations-vocabulary precision, 54809be the battery + the evidence record — the worker delivery, and 16eb274 the TECH-LEAD-AUTHORED case_68 resolution amendment (the DEC-0122 forward rule applied — see case_68_resolution below); no rebase, no force-push, no amending; the PR head at merge IS the reviewed head) verified at the integration station (never trust reported numbers — every gate re-run): (1) the EXACT DELIVERY HEAD 54809be (phase 1, 2026-09-13T00:59-01:00Z): the governance gates 6/6 PASS — current_spec_check, tech_lead_guard, architecture_drift_guard (implementation-only, 4 files, 0 control-plane), fresh_session_check (PR context, pre-merge main), authorization_provenance (delta covered by R9-CORE-001, byte-identical authorization inheritance), spec_check_selftest; the M022 battery tools/satellite_selftest.py 17/17 x3 byte-identical; the five R8 batteries at their accepted counts ALL GREEN; the adapter battery 69/70 with EXACTLY case_68 failing (the pre-disclosed conflict — see below); the PR CI run 34729025939 red on EXACTLY the adapter step (verified via the jobs API) — the expected pre-acceptance shape, identical to the accepted PR #45's archived run. (2) the EXACT AMENDED HEAD 16eb274 (post-amendment re-verification): the adapter battery 70/70 (case_68 PASSING with the evolved expected set); the satellite battery 17/17 x3 byte-identical re-run; accesstech 19/19; wireline 14/14; the five R8 batteries at their accepted counts; all six governance gates rc=0. (3) the MERGE STATE (the actual merge commit 68060d9, direct verification): all gates rc=0 except the fresh_session_check pre-acceptance transient this reconciliation closes; every battery green (adapter 70/70, satellite 17/17, accesstech 19/19, wireline 14/14, the five R8 batteries at their accepted counts). (4) CI: run 34730611261 SUCCESS on the amended delivery head 16eb274; the merge-commit run 34730916577 on 68060d9 verified green before this governance push (the DEC-0117/DEC-0118/DEC-0121/DEC-0122 precedent). Scope verified 4 charter files at the delivery head (+5021/-0: adapters/reference/satellite.py NEW +702, accesstech/satellite.py NEW +1083, tools/satellite_selftest.py NEW +2828, docs/M022-evidence.md NEW +407) plus the shared-battery-surface evolution (tools/adapter_selftest.py +4/-1, the case_68 expected-set addition under the R9-CORE-001 tools/ scope) — the charter boundary honored, the battery evolution sanctioned per the DEC-0122 forward rule. The review is durably recorded in this decision (GitHub self-approval was unavailable — the single operator account owns both the PR and the review — so this decision is the authoritative acceptance record per repo convention)."

decision: ACCEPTED

delivery:
  pr: 46
  branch: "m022-satellite"
  baseline_sha: 9ee861126b2046ea8fb1ea698ba8b0554cac8452
  pr_head: 16eb2748892b5db40e26fbe5db7e612dd1f4b0d0
  reviewed_sha: 16eb2748892b5db40e26fbe5db7e612dd1f4b0d0
  merge_sha: 68060d9cb42bfe24c06eee08376fb29a4194a328
  merged_at: "2026-09-13T01:31:38Z"
  ci_run: 34730611261
  merge_ci_run: 34730916577
  worker_session: "m022-satellite (delivered through the CHARTER-FOREKNOWLEDGE session 51605b0e: the 5th churn dispatch (00:02Z) after the evening queue crunch destroyed 4 queued sessions at the bounded cadence; the worker generated ~55 min (the page burst to 380k chars), pushed the branch (3 commits), opened PR #46 with the complete body, and its completion report LANDED IN-CHAT at ~01:20 (unlike the M021 dead-stream) — with the HONEST CI NOTE: the worker reported the real CI FAILURE conclusion (the pre-disclosed case_68 adapter-step red) with the exact failing step and the archived PR #45 shape comparison, instead of the charter template's 'SUCCESS' placeholder — the honesty discipline held; the report's base/head/PR/battery numbers verified against the station's own runs throughout)"

# case_68 RESOLUTION (the DEC-0122 forward rule applied — the second application of the
# charter-sanctioned battery-surface evolution class):
case_68_resolution:
  conflict: "the accepted adapter battery's case_68 freezes the adapters/ delta to the declared M007 set PLUS the accepted R9 children (wireline.py joined at DEC-0122); the M022 charter scope REQUIRES adapters/reference/satellite.py — 69/70 at the delivery head 54809be, verified experimentally by the worker (whose charter PRE-DISCLOSED the conflict: the KNOWN CONFLICT block saved the entire investigation the M021 worker had to perform); the PR CI red on exactly the adapter step (run 34729025939, verified via the jobs API); the worker charter's battery-scope-repair clause correctly left the accepted battery untouched"
  sanction: "the DEC-0122 forward rule (this decision's standing_rules.battery_surface_evolution, itself the R9 charter Consumption rule + the M019 precedent): each R9 child's reference module joins case_68's expected set at ITS OWN acceptance — never before"
  amendment: "commit 16eb274 (Tech-Lead-authored, z-ai-architect, on the delivery branch, pre-merge): case_68's expected_delta gains EXACTLY adapters/reference/satellite.py; append-only, no assertion weakened (the W016 flow, the export tables, the family byte-identity checks unchanged; the guard still rejects every other undeclared adapters/ change); disclosed in docs/M022-evidence.md §0 (TECH LEAD RESOLUTION) + §9 (amendment provenance); re-verified at the amended head: the adapter battery 70/70"
  forward_rule: "the M023 sibling (adapters/reference/futureimt.py) joins the expected set at the M023 acceptance (DEC-0124 under the current numbering) — never before; the m023 worker's charter already carries the conflict foreknowledge (the v2 TECH LEAD ADDENDUM)"

acceptance_effects:
  current_child_advance: "M022 ACCEPTED — the current-child pointer ADVANCES M022 -> M024 ALONG THE CHAIN (Future Access Convergence, the convergence child — the DEC-0115/DEC-0117/DEC-0121/DEC-0122 chain-child pointer precedent; the chain is M020 -> M021 -> M022 -> M024 and M023 is chain-independent, NEVER holding the pointer). M024's M020/M021/M022 chain dependencies are all satisfied; its M023 dependency is PENDING — the chain-independent M023 worker delivery is IN FLIGHT (PR #47 open, the dead-stream/git-channel pattern: the branch pushed and the PR opened before the turn's stream died; the report never landed in-chat) — so M024's dispatch is BLOCKED until the M023 acceptance (the DEC-0117/DEC-0118 precedent class: the chain-independent sibling's acceptance unblocks the chain child WITHOUT moving the pointer). The R9-CORE-001 program authorization STAYS ACTIVE (it closes only with the R9 gate completion at the M024 acceptance)."
  roadmap_version: "2.19 -> 2.20 (M022 COMPLETE — the THIRD R9 chain child accepted; the R9 gate stays ACTIVE with M024 the current child, blocked-on-M023)"
  authorization_state: "R9-CORE-001 stays ACTIVE (the bounded R9 program authorization; the current-child binding advances to M024 in R9.yaml; the accepted-children record gains M022 accepted under DEC-0123)"
  ci_wiring: "The M022 satellite battery step (wired by DEC-0122 with the pre-delivery existence guard) is ACTIVATED by this delivery merge: at the merge state and the accepted head the step finds tools/satellite_selftest.py and runs it — green 17/17. The M023 futureimt battery (tools/futureimt_selftest.py, the M023 charter scope) is CI-wired by THIS acceptance with a pre-delivery existence guard (the DEC-0120/M0121/M0122 wiring precedent) — the step activates at the M023 delivery merge."
  checker_synchronization: "the synchronized checker evolution per the DEC-0120/M0121/M0122 pattern: tools/current_spec_check.py and tools/fresh_session_check.py advance to the v2.20 markers, the active-work-item marker M022 -> M024, and the M022 child-acceptance marker appended to the R9 child_acceptance_decisions chain (control-plane, governance-only — the drift guard's CONTROL_FILES freeze holds against implementation PRs)"
  ledger: "M022 accepted-merged entry appended + LEDGER-RECON-037 (the snapshot advances 8ebdb21 -> 68060d9 through the DEC-0122 acceptance commit 9ee8611 and the PR #46 delivery merge; this governance commit sits beyond the new pin per the standing reconciliation convention, enforced by tools/fresh_session_check.py)"

verification_summary:
  battery: "tools/satellite_selftest.py 17/17 x3 consecutive runs with byte-identical logs at the delivery head 54809be AND re-run at the amended head 16eb274 (2026-09-13 station verification); green at the merge state 68060d9 (the DEC-0122 existence guard ACTIVATED there)"
  full_suite: "at the delivery head: every battery green except the pre-disclosed case_68 (adapter 69/70, exactly that case); at the amended head and the merge state: adapter 70/70, satellite 17/17, accesstech 19/19, wireline 14/14, the five R8 batteries at their accepted counts (scale 53/53, credential 32/32, recovery 36/36, resilience 36/36, localfirst 35/35); the six governance gates rc=0 (the merge-state fresh_session_check reading at the pre-acceptance moment is exactly the standing transient this reconciliation closes)"
  gates: "current_spec_check PASS; tech_lead_guard PASS; architecture_drift_guard PASS (implementation-only, 4 files + the sanctioned shared-battery evolution, 0 control-plane); fresh_session_check PASS (PR context pre-merge; the merge-state transient closed by this reconciliation); authorization_provenance PASS (delta covered by R9-CORE-001); spec_check_selftest PASS"
  ci: "run 34729025939 FAILURE on the delivery head 54809be — red on EXACTLY the adapter step (the pre-disclosed case_68; every other step green — verified via the jobs API, the identical pre-acceptance shape as the accepted PR #45's archived run 34720307053); run 34730611261 SUCCESS on the amended head 16eb274 (the adapter step green with the evolved case_68); the merge-commit run 34730916577 on 68060d9 SUCCESS, verified green before this governance push"
  scope: "4 charter files at the delivery head, +5021/-0: adapters/reference/satellite.py NEW (702), accesstech/satellite.py NEW (1083), tools/satellite_selftest.py NEW (2828, 17 cases), docs/M022-evidence.md NEW (407 + the resolution addendum) + tools/adapter_selftest.py +4/-1 (the case_68 expected-set evolution — the R9-CORE-001 shared tools/ scope, the DEC-0122 forward rule, no assertion weakened)"

physical_obligations: "EVID-002..EVID-008 remain OPEN and untouched; SOFTWARE-class deterministic evaluation only; R4/W040 remains independently in-review and unaccepted"

standing_rules:
  w048: "accepted-not-restored; MUST NOT be restored implicitly or explicitly"
  architecture: "Architecture 1.1 remains the sole forward design source; the R9 charter by-reference consumption rule is an acceptance criterion at every remaining R9 child (no accepted authority reimplemented, weakened, forked, or bypassed); the gate's own objective constraint holds — access technologies added or replaced through the adapter boundary WITHOUT altering the connectivity contract core (LOCK-101)"
  battery_surface_evolution: "the case_68 expected-set forward rule (carried from DEC-0122, applied here for satellite.py): each R9 child's reference module joins the adapter battery's declared set at its OWN acceptance — never before; the disclosed-evolution class remains append-only-in-intent and never-assertion-weakening (the M019/R9-charter sanction); the M023 futureimt.py sibling joins at the M023 acceptance"
  history: "No work-item history is rewritten; the M022 ledger entry is appended, never converted; the M008 docstring-only disclosures in sessions/, mobility/, multipath/ stay exactly as accepted"
