# DEC-0118 — M018 acceptance: Credential and Key Lifecycle Operations accepted (chain-independent); the current-child pointer does NOT move — M019 stays current and becomes UNBLOCKED
# Sole-Architect governance decision (per-child acceptance, the DEC-0114 R8 serialization point; the FOURTH R8 child accepted —
# CHAIN-INDEPENDENT, the M013/DEC-0113 precedent class: the chain is M015 -> M016 -> M017 -> M019 with M018 outside the chain,
# branching from the accepted R7 state and acceptable in any order relative to M015-M017).
decision_id: DEC-0118
date: "2026-09-12"
type: acceptance
status: ACCEPTED
owner: "Architect"
work_item: M018
authorization: R8-CORE-001

review_basis: "Tech Lead review of the exact M018 delivery head dba0e9aac0e1bf8c00ff1445c01a1603f25161b4 (PR #42, branch m018-credentials, base bda91c3 — the DEC-0116 M016-acceptance head, the live main at the 01:08Z dispatch: the accepted-R7-state rooting rule, the R8 overlay rule — it branches from the accepted R7 state and may be accepted in any order relative to M015-M017; three append-only worker-authored commits, no rebase/force-push/amending, delivered across TWO worker sessions under the continuation charter — see continuation_disclosure; the PR head IS the reviewed head, no tip advance) verified under the CHAIN-INDEPENDENT VERIFICATION PROTOCOL (the M013/DEC-0113 precedent class, the decisive review method whenever main has advanced past a chain-independent branch's root): (1) the BASE-CONSISTENT ref (origin/main = bda91c3, the branch root): the governance gates 5/5 PASS — current_spec_check, tech_lead_guard, architecture_drift_guard (implementation-only 11 files, 0 control-plane), fresh_session_check (PASS with main verified against the execution-state snapshot bda91c3 — the branch-carried snapshot pins a0ebd019 with the DEC-0116 governance commit beyond it per the standing convention), authorization_provenance (11/11 delta files covered by R8-CORE-001, baseline 28b3150, byte-identical authorization inheritance); (2) the exact head at the LIVE main (d16093d at review time — DEC-0117 landed on main while this branch was in flight, and the branch correctly never rebased): the bare-head suite shows 35 steps rc=1 — EVERY failing case is the frozen-spec-unchanged / PR-delta-shape comparison class (each affected battery failing exactly its 1-2 comparison cases: adapter 69/70, resilience 35/36, client 23/24, usage 51/53, allocation 60/62 — ALL content cases green); the comparison cases diff the working tree against origin/main and see DEC-0117's governance files (advanced past the branch's base) as though this PR's delta touched them, and fresh_session_check at the live main objects to the then-unreconciled post-merge main state (main past the branch root with the M017 delivery merge 438acb66 — implementation-domain files cited) — the pending acceptance reconciliation, NOT this PR's content, exactly the DEC-0113 precedent ('fresh_session_check at review time objects to the then-unreconciled post-merge main state'); (3) the CI-EQUIVALENT MERGE STATE (the decisive state — what the pull_request CI checks out at refs/pull/42/merge): the clean automatic merge into the live main (ZERO conflicts — the scope disjointness held: the merge-base three-dot delta is exactly the 11 M018 files), ALL R8 batteries green re-run at the station merge state (credential 32/32, recovery 36/36, resilience 36/36, localfirst 35/35 — the two-dot environment-sensitive sibling cases green exactly where CI runs them), the worker's full merge-state suite 66 batteries ALL GREEN at their accepted counts, and CI BOTH JOBS SUCCESS on the PR (run 34669231951 on the exact head dba0e9a — the pull_request merge-ref checkout, where the M018 credential battery step wired by DEC-0117 ACTIVATED: the existence guard found tools/credential_selftest.py and ran it green). The battery (tools/credential_selftest.py, 32 deterministic offline seeded cases covering the charter's (a)-(f) acceptance matrix) 32/32 PASS run 3x consecutively with byte-identical logs at the exact head (2026-09-12T03:08-03:13Z station verification); cross-PYTHONHASHSEED determinism verified in-battery (case_27); the battery output md5-identical across BOTH delivery sessions' runs (55a3f16d6fe708bed423e68503dfcbad — the continuation session's battery is byte-for-byte the milestone-1 battery with only the disclosed case_31 fix applied). Scope verified 11 files, +6783/-0 — credentials/ x9 NEW (errors.py 102 — the frozen credential-* typed reason vocabulary, 11 fail-closed codes, the zero-coverage-gap credential-rotation-nonatomic, the declared-bound credential-propagation-unconverged, the fail-closed inventory credential-inventory-incomplete, consumed-domain errors wrapped at every boundary with their deterministic text preserved; model.py 1374 — the typed records with the journaled before/after ADMITTED SETS, the position-independent content identity as the idempotency key, the kind-conditional member discipline, the frozen vocabularies with import-time drift guards over the consumed M014 verdict subsets, and the pure kernels (derive_operation_id, the atomicity kernels, check_zero_coverage_gap, admitted_set_from_records, round_deliveries, plan_propagation); journal.py 259 — the deterministic idempotent replayable tamper-evident lifecycle journal whose fold MECHANICALLY enforces the zero-coverage-gap invariants at replay (gapless positions, unique identities, CONTINUITY — every record's before-set equals its predecessor's after-set, so no operation window goes unaccounted); rotation.py 300 — the rotation drills driving the accepted IdentityService.rotate (the authority's own all-or-nothing StoreBatch) BY REFERENCE with the authority outcome mechanically verified as an exact single flip BEFORE journaling; propagation.py 208 — the revocation propagation as deterministic rounds (a pure function of the declared plan, every round journaled, the DECLARED ROUND-COUNT bound enforced, an unconvergeable plan failing closed with its bounded rounds staying journaled as the evidence of the bounded failure); emergency.py 339 — the emergency revocation as a DISTINCT TYPED JOURNALED operation (exactly the three frozen steps in order through the accepted authorities — the identity revocation, the M014 authorization revocation, the accepted emergency stop driven through the frozen W049 client's OWN emergency_stop control — with verify_emergency_evidence failing closed unless every step's LOCK-106 references resolve in the REAL evidence store); inventory.py 321 — the fail-closed credential inventory audit (an admitted credential without a complete lifecycle record chain is the typed credential-inventory-incomplete failure citing the break kind, never a warning — a case per break kind); gate.py 284 — the admission gate consumed BY REFERENCE from the accepted client//federation/ surfaces (LOCK-117 — no second authorization runtime: the M014 credential-lifecycle verdict, the M014 principal-authorization registry verdict and the converged traffic-admission point driven with a ZERO-byte probe, the composed verdict mechanical, a non-accepted gate object rejected); __init__.py 175 — the public surface, 42 names), tools/credential_selftest.py NEW (3015 lines, 32 cases), docs/M018-evidence.md NEW (406-line SOFTWARE-class matrix) — every delta file covered by the R8-CORE-001 M018 scope entries. The structural invariants verified (rotation drills with ZERO COVERAGE GAPS — the admitted-set transition ATOMIC and JOURNALED, no window admits the revoked or superseded credential, verified at construction AND at replay, the wire-forged non-atomic shapes failing closed; revocation propagation as deterministic rounds with declared convergence bounds expressed as ROUND COUNTS — never wall clock, LOCK-119; emergency revocation EXPLICIT, TYPED, JOURNALED, EVIDENCE-VISIBLE — LOCK-106 — never a silent shortcut, the ordinary revocation kind structurally unable to carry the emergency steps; credential inventory verification FAIL-CLOSEN — every admitted credential traces to a valid lifecycle record, a broken chain a typed failure with a case per break kind; key material NEVER STORED, LOGGED OR ECHOED — LOCK-119 — the accepted identity/ CredentialStore boundary never opened (get_secret/put_secret AST-absent), no bytes-typed member on any public record, the battery fixtures assembled at runtime from fragments so the full secret shape never appears in source, the battery's own secret-hygiene case; the admission gate consumed BY REFERENCE — LOCK-117; one-way imports — no accepted authority imports credentials/, the M015-M017 chain siblings neither imported nor touched, the chain-independent composition); ONE disclosed battery defect fix (687fb825b3 — the case_31 PR-delta computation defect, see delivery_repairs_disclosed — in-scope, its own append-only commit, no assertion weakened, the battery green at the fixed head); the accepted sibling batteries at their accepted counts in the merge state (recovery 36/36, resilience 36/36, localfirst 35/35 among them). The review is durably recorded in the PR #42 review timeline (review comment 5184993797, COMMENTED state, posted 2026-09-12T03:16:52Z); GitHub self-approval was unavailable (the single operator account owns both the PR and the review), so this decision is the authoritative acceptance record per repo convention."

decision: ACCEPTED

delivery:
  pr: 42
  branch: "m018-credentials"
  baseline_sha: bda91c39fb80c89dc3df1eaf1864f6560558a16b
  pr_head: dba0e9aac0e1bf8c00ff1445c01a1603f25161b4
  reviewed_sha: dba0e9aac0e1bf8c00ff1445c01a1603f25161b4
  merge_sha: e037ca8de7379f3f6d461a2aa9994442ca145e4f
  merged_at: "2026-09-12T03:17:00Z"
  ci_run: 34669231951
  merge_ci_run: 34669945963
  worker_session: "m018-credentials (the full M018 charter delivered across TWO worker sessions on the append-only branch under the continuation charter — the M015 precedent class: session 1 (b25e1a87, dispatched 2026-09-12T01:08Z) delivered the complete credentials/ domain + the 32-case battery + the evidence record in ONE commit (baa28da, 01:51:05Z), opened PR #42 at 02:11:19Z and posted its verification numbers (32/32 battery, the 66-battery merge-state suite green, gates PASS at the then-current main), then its turn ZOMBIED server-side (the boot-rule #23 signature — see continuation_disclosure); the CONTINUATION session (5d5716f0, dispatched 02:46Z) verified milestone 1 end to end, delivered the disclosed case_31 defect fix (687fb82, 02:53:10Z) + the evidence record's continuation update (dba0e9a, 02:54:23Z) + the continuation verification numbers (03:02Z); no rebase, no force-push, no amending — the branch never re-rooted from bda91c3)"

# TWO-SESSION CONTINUATION DISCLOSURE (modeled on DEC-0115's continuation_disclosure —
# the same continuation-charter recovery class): the capacity peak destroyed the first
# session's sandbox mid-delivery AFTER its milestone-1 push, and the continuation
# charter recovered the delivery from the append-only branch. ONE disclosed battery
# defect fix (see delivery_repairs_disclosed).
continuation_disclosure:
  session_1: "b25e1a87 (dispatched 2026-09-12T01:08Z under the Task 56 M018 charter, the parallel Worker B) — delivered the COMPLETE milestone 1 in one commit (baa28da at 01:51:05Z: the credentials/ domain x9, the full 32-case tools/credential_selftest.py, docs/M018-evidence.md — the commit-as-you-go discipline, the git-delivery gold standard), opened PR #42 at 02:11:19Z and posted its verification numbers (the 32/32 battery, the 66-battery merge-state suite green, gates PASS at the then-current main) — then its turn ZOMBIED server-side: the boot-rule #23 signature (the assistant placeholder opened at 01:09 never closed, ZERO server-side message-tree growth while the branch advanced); a continuation nudge delivered to the wedged session parked as a SERVER-SIDE DRAFT (trap 19(c) — the send landed page-verified but the message tree stayed at 2 messages, the chat's updated_at jumping without a new message — blocked by the open zombie turn); the session's sandbox was RELEASED at 02:40Z during the parallel M019 dispatch (the capacity-peak destruction cycle — the dispatcher's release check found no active m018 job), destroying the in-progress battery work while milestone 1 stayed SAFE on the branch"
  session_2: "5d5716f0 (dispatched 02:46Z under the CONTINUATION charter rewritten at the Tech Lead station — the M015 continuation precedent class: branch from the EXISTING m018-credentials HEAD baa28da, append-only, never re-root, the branch-advancement fallback making future re-dispatches idempotent; verify milestone 1 first — every credentials/ file read, the import check, the defect-fix discipline for in-scope fixes) — verified milestone 1 against the accepted authorities, exposed and fixed the ONE disclosed battery defect (the case_31 PR-delta computation, 687fb82 — §4 of the evidence record), delivered the evidence record's continuation update (dba0e9a — the two-session history, the disclosed defect fix, the main-advancement disclosure), and posted the continuation verification numbers at 03:02Z (the battery x3 byte-identical, md5 55a3f16d6fe708bed423e68503dfcbad — identical to session 1's run)"
  capacity_context: "the capacity-peak destruction cycle hit the m018 delivery in its ZOMBIE form (trap #23 — the turn that never closes server-side) rather than M015's pod-outlives-tab destruction or M017's tab-destruction form: the milestone-1 work was never lost (the append-only branch carried it — the git-delivery gold standard doing its job), the zombie session was VOIDED with the full reason before the continuation dispatch, and nothing was rebased, force-pushed, or amended; the continuation charter recovered the delivery exactly as it recovered M015 — the second execution of the proven playbook"

delivery_repairs_disclosed:
  - commit: 687fb825b311bfd5e40506f1a223512b5d28337f
    summary: "the case_31 PR-delta computation defect (tools/credential_selftest.py — the MILESTONE-1 battery, exposed by the continuation session's milestone-1 verification): the delivered case computed the PR delta with a plain two-dot git diff --name-only origin/main (main's HEAD vs the working tree) — a form that is environment-sensitive on a chain-independent branch: it conflates the two diff directions whenever main has advanced past the branch root (DEC-0117 landed while the branch was in flight), reporting main-side governance files (the M017 acceptance's spec/, .github/, AGENTS.md, README.md changes) as though this branch's PR touched them — a false negative in one environment (the local non-rebased checkout), i.e. a real defect in the check's construction; the fix computes the delta with the MERGE-BASE (three-dot) semantics every authoritative consumer already uses (the CI provenance step's git diff --name-only origin/main...HEAD and the charter's own zero-overlap form), plus the uncommitted working-tree diff and untracked files (local pre-commit hygiene)"
    verified: "in-scope tools/ battery repair, its own append-only commit (687fb82), disclosed in docs/M018-evidence.md §4 before acceptance; NO assertion weakened — the spec//.github control-plane rejection and the active-authorization coverage check unchanged; the battery green at the fixed head (dba0e9a); the same root cause in the ACCEPTED sibling batteries' delta-shape cases (resilience case_35, localfirst case_34, the recovery battery's equivalent) disclosed as OUT OF SCOPE for this delivery (green at their accepted counts at the CI merge ref and in the merge state, verified by direct execution — not patched, the charter's battery-scope-repair stop rule)"

acceptance_effects:
  current_child_does_not_move: "M019 REMAINS the current chain child (the CHAIN-INDEPENDENT acceptance — the M009/M013 precedent: a chain-independent child's acceptance does not move the current-child pointer; the chain is M015 -> M016 -> M017 -> M019 with M018 outside it, and the current-child pointer follows the CHAIN). M018's acceptance UNBLOCKS M019: its declared dependencies M015+M016+M017+M018 are now ALL accepted — M019's acceptance is no longer blocked, the in-flight M019 worker's merge may proceed, and the M019 acceptance (DEC-0119) is the R8 gate completion carrier (the R8-CORE-001 program authorization STAYS ACTIVE — it closes only with the R8 gate completion at the M019 acceptance, the DEC-0088/DEC-0108 mid-chain precedent); only M019 remains between the R8 gate and completion"
  roadmap_version: "2.14 -> 2.15 (M018 COMPLETE — the FOURTH R8 child accepted; the R8 gate stays ACTIVE with M019 the current child, now unblocked — only M019 remains)"
  authorization_state: "R8-CORE-001 stays ACTIVE (the bounded R8 program authorization; the current-child binding STAYS M019 in R8.yaml — now with the unblocked note; the accepted-children record gains M018 accepted under DEC-0118 with M019 the remaining child)"
  ci_wiring: "The M018 credential battery step (wired by DEC-0117 with the pre-delivery existence guard) is ACTIVATED by this delivery merge: at the merge state and the accepted head the step finds tools/credential_selftest.py and runs it — green 32/32 (the PR CI run 34669231951 executed it at the pull_request merge ref; the station merge-state run confirmed 32/32); NO new battery guard is wired by this acceptance — M019's battery is the DISCLOSED EVOLUTION of the already-wired tools/scale_selftest.py (the DEC-0117 wiring note already recorded this — the M014 precedent class; restated so the wiring history stays honest)"
  ledger: "M018 accepted-merged entry appended + LEDGER-RECON-032 (the snapshot advances 438acb66 -> e037ca8d over the authorization-era span 28b3150 -> b0145f1 -> d77a561 -> 6b96438 -> a0ebd019 -> bda91c3 -> 438acb66 -> d16093d -> e037ca8d, the DEC-0109/DEC-0114 RECON semantics — the prior DEC-0117 acceptance commit d16093d is brought into the pin by this governance action; NOTE the M018 branch's own base was bda91c3, the CHAIN-INDEPENDENT rooting — the branch point sits earlier on the span while the ledger pin advances along main; this governance commit sits beyond the new pin per the standing reconciliation convention)"

verification_summary:
  battery: "tools/credential_selftest.py 32/32 x3 consecutive runs with byte-identical logs at the exact delivery head dba0e9a (2026-09-12T03:08-03:13Z station verification); cross-PYTHONHASHSEED determinism verified in-battery (case_27); the battery output md5-identical across both delivery sessions' runs (55a3f16d6fe708bed423e68503dfcbad)"
  full_suite: "the full workflow suite in exact CI order in the CI-EQUIVALENT MERGE STATE (the decisive state) — 66 batteries ALL GREEN at their accepted counts (the M018 credential step ACTIVATED by this delivery at the merge state; the M017 recovery step live from main) with exactly the one documented non-zero exit: the legacy spec_check.py compatibility audit baseline (continue-on-error: true); at the BARE head with the live main the suite shows the UNRECONCILED-MAIN ARTIFACT — 35 steps rc=1, every failing case the frozen-spec-unchanged / PR-delta-shape comparison class (adapter 69/70, resilience 35/36, client 23/24, usage 51/53, allocation 60/62 — ALL content cases green), the two-dot comparison cases seeing DEC-0117's governance files as this PR's delta — the DEC-0113 precedent, green exactly where CI runs them (the merge state)"
  gates: "base-consistent ref (origin/main = bda91c3): current_spec_check PASS; tech_lead_guard PASS; architecture_drift_guard PASS (implementation-only, 11 files, 0 control-plane); fresh_session_check PASS (main verified against the execution-state snapshot bda91c3); authorization_provenance PASS (11/11 covered by R8-CORE-001: credentials/, tools/credential_selftest.py, docs/M018-evidence.md); at the live main the fresh-session/provenance objections are the unreconciled-main artifact (the pending acceptance reconciliation, NOT this PR's content — the DEC-0113 precedent)"
  ci: "run 34669231951 SUCCESS on the exact delivery head dba0e9a (both jobs — the spec-check job and the platform-capability job; the pull_request merge-ref checkout where the M018 credential battery step ran green); the merge-commit run 34669945963 on e037ca8d in_progress at package-assembly time — the Tech Lead re-verifies green before push (the DEC-0117 precedent); the milestone-1 PR-open run 34666961951 (baa28da, 02:11:32Z) failed the fresh-session step in the unreconciled window — main's then-head 438acb66 (the M017 merge) before the DEC-0117 acceptance commit landed, the same DEC-0113 artifact, not the PR's content; the merge is a clean automatic merge (zero conflicts) of a branch rooted at bda91c3 with the merge-base three-dot delta exactly the 11 M018 files"
  scope: "11 files, +6783/-0 at the reviewed head: credentials/ x9 NEW (errors.py 102, model.py 1374, journal.py 259, gate.py 284, rotation.py 300, propagation.py 208, emergency.py 339, inventory.py 321, __init__.py 175), tools/credential_selftest.py NEW (3015 lines, 32 cases), docs/M018-evidence.md NEW (406 lines) — the charter boundary honored exactly; ONE disclosed battery defect fix (687fb825b3 — the case_31 PR-delta computation, in-scope tools/, its own append-only commit, no assertion weakened)"

physical_obligations: "EVID-002..EVID-008 remain OPEN and untouched; SOFTWARE-class deterministic evaluation only; R4/W040 remains independently in-review and unaccepted"

standing_rules:
  w048: "accepted-not-restored; MUST NOT be restored implicitly or explicitly"
  architecture: "Architecture 1.1 remains the sole forward design source; the R8 charter by-reference consumption rule is an acceptance criterion at every remaining R8 child (no accepted authority reimplemented, weakened, forked, or bypassed)"
  history: "No work-item history is rewritten; the M018 ledger entry is appended, never converted; the M008 docstring-only disclosures in sessions/, mobility/, multipath/ stay exactly as accepted"
