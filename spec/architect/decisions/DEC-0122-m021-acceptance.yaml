# DEC-0122 — M021 acceptance: Wireline Access Adapters accepted (the SECOND R9 chain child);
# the current-child pointer ADVANCES M021 -> M022
# Sole-Architect governance decision (per-child acceptance, the DEC-0120 R9 serialization point;
# the DEC-0115/DEC-0117/DEC-0121 chain-child precedent class: the chain is M020 -> M021 -> M022 -> M024 with
# M023 chain-independent, and the current-child pointer follows the CHAIN).
decision_id: DEC-0122
date: "2026-09-12"
type: acceptance
status: ACCEPTED
owner: "Architect"
work_item: M021
authorization: R9-CORE-001

review_basis: "Tech Lead review of the exact M021 delivery head 89666931a4e9a3ad30b8b0cd0db9d54762c93a1f (PR #45, branch m021-wireline rooted at the live DEC-0121 M020-acceptance head fe27c9962f5e5ad4da9b355bf3be1172825bf4fe — the live-main rule, no root-behind-main reconciliation needed; two append-only commits: b0e7dfd the WORKER delivery (the wireline reference compositions, the accesstech wireline declarations, the battery and the evidence record) and 8966693 the TECH-LEAD-AUTHORED case_68 resolution amendment (see case_68_resolution below) — no rebase, no force-push, no amending; the PR head at merge IS the reviewed head, no tip advance beyond the amendment itself) verified at the integration station (never trust reported numbers — every gate re-run): (1) the EXACT AMENDED HEAD: the governance gates 6/6 PASS — current_spec_check, tech_lead_guard, architecture_drift_guard (implementation-only), fresh_session_check (PASS in the PR context at the pre-merge main), authorization_provenance (delta files covered by R9-CORE-001, byte-identical authorization inheritance), spec_check_selftest; the M021 battery tools/wireline_selftest.py 14/14 PASS run 3x consecutively with byte-identical logs; the accepted adapter battery tools/adapter_selftest.py 70/70 (case_68 PASSING with the evolved expected-set); the accesstech battery 19/19; the five accepted R8 batteries at their accepted counts ALL GREEN (scale 53/53, credential 32/32, recovery 36/36, resilience 36/36, localfirst 35/35). (2) the MERGE STATE (the decisive CI-equivalent state — the actual merge commit 8ebdb21, not a local simulation): all six gates rc=0 except fresh_session_check reporting EXACTLY the expected pre-acceptance transient ('main advanced beyond the snapshot c75a7c70 with implementation-domain changes; reconcile the execution-state snapshot before implementation' — the reconciliation THIS commit performs; push events skip the step by construction, the DEC-0121-equivalent transient); the adapter battery 70/70, the wireline battery 14/14, accesstech 19/19, the five R8 batteries at their accepted counts — ALL GREEN at the merge commit. (3) CI: run 34722578604 SUCCESS on the amended delivery head 8966693 (the adapter step green with the evolved case_68); the merge-commit run 34723075971 on 8ebdb21 verified green before this governance push (the DEC-0117/DEC-0118/DEC-0121 precedent). Scope verified 5 files at the reviewed head (+3333/-1) — the four charter files (adapters/reference/wireline.py NEW +779, accesstech/wireline.py NEW +304, tools/wireline_selftest.py NEW +1855, docs/M021-evidence.md NEW +351+addendum) plus the shared-battery-surface evolution (tools/adapter_selftest.py +13/-1, the case_68 expected-set addition under the R9-CORE-001 tools/ scope) — the charter boundary honored, the battery evolution sanctioned per the Consumption rule. The review is durably recorded in this decision (GitHub self-approval was unavailable — the single operator account owns both the PR and the review — so this decision is the authoritative acceptance record per repo convention)."

decision: ACCEPTED

delivery:
  pr: 45
  branch: "m021-wireline"
  baseline_sha: fe27c9962f5e5ad4da9b355bf3be1172825bf4fe
  pr_head: 89666931a4e9a3ad30b8b0cd0db9d54762c93a1f
  reviewed_sha: 89666931a4e9a3ad30b8b0cd0db9d54762c93a1f
  merge_sha: 8ebdb21b79a6e53bc45c8b1ec789c960a0bd1b5c
  merged_at: "2026-09-12T22:48:30Z"
  ci_run: 34722578604
  merge_ci_run: 34723075971
  worker_session: "m021-wireline (delivered through the DEAD-STREAM session 28805224: dispatched 20:44Z when the ~8.9-hour capacity-peak destructive phase finally eased — the churn survived by the corrected treadmill guards (the server-side-proven reset, the 5-destroy 60-min pause, the rate-limit hard freeze); the worker ran a ~47-minute turn — deep analysis (the charter-conflict investigation that anticipated case_68, the M019 precedent study, the R9/DEC-0121 governance probing), then implementation (the wireline compositions, the accesstech declarations, the 14-case battery written and debugged, the M020 battery confirmed green in-flight at 19/19), the branch push, PR #45 with the complete report body — then the turn's event stream died mid-report ~21:32Z (the server-side message tree stays (2, 12926) = charter + empty assistant: the streamed events are client-transient and the content never landed; the m017/M019 wedged-render precedent class in its terminal form). The git/PR channel carried the COMPLETE delivery — the evidence record §0 DISCLOSED CONFLICT (case_68, experimentally root-caused with a probe-commit proof) and the PR body's full verification report; no in-chat completion report ever landed; the resident harvested the delivery from the git channel per the gold standard and the chat was closed post-merge (voided, deleted server-side) to free the account generation slot for the in-flight M023 worker)"

# case_68 RESOLUTION (the R9 charter's own declared path — recorded here as the authoritative
# sanction for the one battery-surface evolution this acceptance carries):
case_68_resolution:
  conflict: "the accepted M007 adapter battery's case_68 freezes the adapters/ delta vs origin/main to exactly the nine declared M007 files (expected_delta); the M021 charter scope REQUIRES the new adapters/reference/wireline.py, so the delivery head b0e7dfd read 69/70 with case_68 failing — VERIFIED experimentally by the worker (probe commit -> 69/70 with exactly that case -> revert -> 70/70) and disclosed in the evidence record §0 + the PR body; the worker charter's battery-scope-repair clause correctly left the accepted battery untouched (the resolution belongs to the Tech Lead)"
  sanction: "the R9 charter's Consumption rule (live charter text, ratified by DEC-0120 at activation): 'a disclosed evolution of an accepted shared battery surface follows the M019 tools/scale_selftest.py precedent — disclosed, append-only in intent, never assertion-weakening'"
  amendment: "commit 8966693 (Tech-Lead-authored, z-ai-architect, on the delivery branch, pre-merge): case_68's expected_delta gains EXACTLY adapters/reference/wireline.py; the W016 canonical flow, the 54-name baseline export table, the family export counts and the six family-subpackage byte-identity checks are UNCHANGED; the guard still rejects every other undeclared adapters/ change; disclosed in docs/M021-evidence.md §0 (TECH LEAD RESOLUTION) + §9 (amendment provenance); re-verified at the amended head: the adapter battery 70/70"
  forward_rule: "each remaining R9 child's reference module joins the expected set at ITS OWN acceptance — M022's adapters/reference/satellite.py at the M022 acceptance, M023's adapters/reference/futureimt.py at the M023 acceptance — never before (pre-adding them would weaken the guard for unaccepted deliveries)"

acceptance_effects:
  current_child_advance: "M021 ACCEPTED — the current-child pointer ADVANCES M021 -> M022 (Non-Terrestrial Access Adapters, the chain child requiring M020 and M021 — both declared dependencies now satisfied; the DEC-0115/DEC-0117/DEC-0121 chain-child pointer precedent). M023 (Future Technology Extension Drill) remains chain-independent and IN FLIGHT as a parallel worker delivery (dispatched 21:02Z, queued-capacity; the M013/M018 parallel precedent class); M024 (the convergence child) requires all four. The R9-CORE-001 program authorization STAYS ACTIVE (it closes only with the R9 gate completion at the M024 acceptance, the DEC-0088/DEC-0108 mid-chain precedent)."
  roadmap_version: "2.18 -> 2.19 (M021 COMPLETE — the SECOND R9 child accepted; the R9 gate stays ACTIVE with M022 the current child)"
  authorization_state: "R9-CORE-001 stays ACTIVE (the bounded R9 program authorization; the current-child binding advances to M022 in R9.yaml; the accepted-children record gains M021 accepted under DEC-0122)"
  ci_wiring: "The M021 wireline battery step (wired by DEC-0121 with the pre-delivery existence guard) is ACTIVATED by this delivery merge: at the merge state and the accepted head the step finds tools/wireline_selftest.py and runs it — green 14/14 (the PR CI run 34722578604 executed it; the station merge-state run confirmed 14/14). The M022 satellite battery (tools/satellite_selftest.py, the M022 charter scope) is CI-wired by THIS acceptance with a pre-delivery existence guard (the DEC-0120/M0121 wiring precedent, the M002/M015 contract-battery class)."
  checker_synchronization: "the synchronized checker evolution per the DEC-0120/M0121 pattern: tools/current_spec_check.py and tools/fresh_session_check.py advance to the v2.19 markers, the active-work-item marker M021 -> M022, and the M021 child-acceptance marker appended to the R9 child_acceptance_decisions chain (control-plane, governance-only — the drift guard's CONTROL_FILES freeze holds against implementation PRs)"
  ledger: "M021 accepted-merged entry appended + LEDGER-RECON-036 (the snapshot advances c75a7c70 -> 8ebdb21 through the DEC-0121 acceptance commit fe27c99 and the PR #45 delivery merge; this governance commit sits beyond the new pin per the standing reconciliation convention, enforced by tools/fresh_session_check.py)"

verification_summary:
  battery: "tools/wireline_selftest.py 14/14 x3 consecutive runs with byte-identical logs at the exact amended head 8966693 (2026-09-12T22:13-22:22Z station verification); green again at the merge state 8ebdb21 (the DEC-0121 existence guard ACTIVATED there); re-run green post-evidence-addendum (the battery's honesty case re-checked)"
  full_suite: "at the amended head: the adapter battery 70/70 (the evolved case_68 green), accesstech 19/19, the five R8 batteries at their accepted counts (scale 53/53, credential 32/32, recovery 36/36, resilience 36/36, localfirst 35/35), the six governance gates rc=0; at the merge state: identical battery results, all gates rc=0 except the fresh_session_check pre-acceptance transient this reconciliation closes (push events skip the step by construction)"
  gates: "amended head: current_spec_check PASS; tech_lead_guard PASS; architecture_drift_guard PASS (implementation-only); fresh_session_check PASS (PR context, pre-merge main); authorization_provenance PASS (delta covered by R9-CORE-001: adapters/reference/wireline.py, accesstech/wireline.py, tools/wireline_selftest.py, docs/M021-evidence.md, and the tools/adapter_selftest.py shared-surface evolution under the tools/ scope); spec_check_selftest PASS"
  ci: "run 34722578604 SUCCESS on the amended head 8966693 (the adapter step green with the evolved case_68 — the resolution validated in CI); the merge-commit run 34723075971 on 8ebdb21 SUCCESS, verified green before this governance push; the merge is a clean merge-commit of a branch rooted at the live main head fe27c99"
  scope: "5 files, +3333/-1 at the reviewed head: the four charter files NEW (adapters/reference/wireline.py 779 — the ethernet/fiber/enterprise-WAN reference compositions; accesstech/wireline.py 304 — the static-availability envelope/ladder/handover declarations through the accepted M020 public surface only; tools/wireline_selftest.py 1855 — the 14-case battery; docs/M021-evidence.md 351+addendum — the SOFTWARE-class matrix with the conflict disclosure and resolution) + tools/adapter_selftest.py +13/-1 (the case_68 expected-set evolution — the R9-CORE-001 shared tools/ scope, the Consumption-rule-sanctioned disclosed evolution, no assertion weakened)"

physical_obligations: "EVID-002..EVID-008 remain OPEN and untouched; SOFTWARE-class deterministic evaluation only; R4/W040 remains independently in-review and unaccepted"

standing_rules:
  w048: "accepted-not-restored; MUST NOT be restored implicitly or explicitly"
  architecture: "Architecture 1.1 remains the sole forward design source; the R9 charter by-reference consumption rule is an acceptance criterion at every remaining R9 child (no accepted authority reimplemented, weakened, forked, or bypassed); the gate's own objective constraint holds — access technologies added or replaced through the adapter boundary WITHOUT altering the connectivity contract core (LOCK-101)"
  battery_surface_evolution: "the case_68 expected-set forward rule (this decision's case_68_resolution.forward_rule): each R9 child's reference module joins the adapter battery's declared set at its OWN acceptance — never before; the disclosed-evolution class remains append-only-in-intent and never-assertion-weakening (the M019/R9-charter sanction)"
  history: "No work-item history is rewritten; the M021 ledger entry is appended, never converted; the M008 docstring-only disclosures in sessions/, mobility/, multipath/ stay exactly as accepted"
