# DEC-0111 — M011 acceptance: Vertical Proof — RoamLink accepted; R7 child M004 unchanged (M011 is a chain-independent vertical proof)
# Sole-Architect governance decision (per-child acceptance, DEC-0101 serialization point).
decision_id: DEC-0111
date: "2026-09-10"
type: acceptance
status: ACCEPTED
owner: "Architect"
work_item: M011
authorization: R7-CORE-001

review_basis: "Tech Lead review of the exact M011 delivery head fb4a921092e114f74bf8d4c54c2d32c230425911 (PR #31, branch m011-roamlink-proof, base ba76496 the live main head at delivery time — the branch was re-rooted from the stale charter-cut 1265e99 to the current head before any commit, per the live-main rule, with zero overlap with the M010 delta): the M011 battery 56/56 PASS run 3x consecutively at the exact head (exit-code-based), the full battery suite verified green at the merge state under CI semantics (62 batteries in exact workflow order; the two non-zero exits are the documented baselines — spec_check.py legacy compatibility audit continue-on-error with the selftest-pinned 7/17 baseline, client_selftest.py the DEC-0099-disclosed containment/W048 skip), CI green end-to-end on the PR head (run 34446263010), scope verified 8 files: seven charter files (roamlink/ six modules, tools/roamlink_selftest.py, docs/M011-evidence.md — all NEW) plus ONE disclosed repair (tools/sharenet_selftest.py +29/-3: the M010 delivery's case_31_pr_delta_shape froze its delta check without the authorization-aware battery-scope consultation the M002-era repairs established across the battery surface — the duty recorded in docs/M001-evidence.md §4; without it every later R7 child delta trips the check; the repair implements exactly the established consultation pattern consulting authorization_provenance.covers(), fail-closed; RATIFIED by the Tech Lead under the active authorization's tools/ scope entry and the recorded duty, properly disclosed in the commit message and PR body — the charter scope-amendment path), the structural invariants verified (LOCK-120 — roamlink/cohort.py the RoamLink application boundary imports ONLY developerapi + stdlib, reviewer-verified import surface + battery AST audit; LOCK-102 — the technology-neutral request carries opaque typed requirement references; LOCK-108 — provider-change absorption preserves the contract hard constraints, case-covered; LOCK-119 clean; LOCK-117 execution artifacts ride as opaque references), the simulation-seam discipline verified (the pending children's mechanics are clearly-labeled deterministic doubles, never imported as authority), and the accepted authorities composed by reference (contracts/, offers/, usage/, commercial/, developerapi/ imported in flow.py/simulation.py, the composition surface; cohort.py the SDK-only boundary). The review is durably recorded in the PR #31 review timeline (review comment 5614479049); GitHub self-approval was unavailable (the single operator account owns both the PR and the review), so this decision is the authoritative acceptance record per repo convention."

decision: ACCEPTED

delivery:
  pr: 31
  branch: "m011-roamlink-proof"
  baseline_sha: ba76496aa57379b0eb48d47798a10dbe16d847a4
  pr_head: fb4a921092e114f74bf8d4c54c2d32c230425911
  reviewed_sha: fb4a921092e114f74bf8d4c54c2d32c230425911
  merge_sha: c0f23c813ea98742316863f21532c4ce2fe5398a
  merged_at: "2026-09-10T06:59:16Z"
  ci_run: 34446263010
  worker_session: "m011-roamlink (charter w23a — the 3-worker pipeline's Worker C)"

acceptance_effects:
  current_child_unchanged: "M004 (M011 is a chain-independent vertical proof, the M013/M010 precedent — vertical proofs do not move the current-child pointer)"
  roadmap_version: "2.2 -> 2.3 (M011 COMPLETE)"
  authorization_state: "R7-CORE-001 stays ACTIVE (the bounded R7 program authorization closes only when the R7 gate completes)"
  ci_wiring: "The roamlink battery is CI-wired by this acceptance (existence-guarded step, the M002/M003/M010 pattern — control-plane action outside worker scope)"
  disclosed_repair_ratified: "tools/sharenet_selftest.py case_31 authorization-aware scope consultation (the M002-era pattern) — RATIFIED under the active authorization's tools/ scope entry and the docs/M001-evidence.md §4 recorded duty; the repair was disclosed in the delivery commit fb4a921 and the PR body before this acceptance"
  ledger: "M011 accepted-merged entry appended + LEDGER-RECON-020 (snapshot baseline ba76496 -> c0f23c8)"

verification_summary:
  battery: "tools/roamlink_selftest.py 56/56 x3 consecutive runs at the exact head"
  full_suite: "62 batteries green in exact CI workflow order at the merge state (CI semantics: spec_check legacy audit continue-on-error baseline 7/17 documented; client battery the DEC-0099-disclosed skip)"
  gates: "current_spec_check PASS; tech_lead_guard PASS; architecture_drift_guard PASS (implementation-only, 8 files); fresh_session_check PASS (pin ba76496 == origin/main at review time); authorization_provenance PASS (8/8 delta files covered by R7-CORE-001)"
  ci: "run 34446263010 SUCCESS on the PR head; the fresh merge inherits the fast-forward-equivalent tree (the branch is rooted at the main head)"
  scope: "8 files: 7 charter (roamlink/ x6, tools/roamlink_selftest.py, docs/M011-evidence.md) + 1 disclosed repair (tools/sharenet_selftest.py) ratified by this decision"

physical_obligations: "EVID-002..EVID-008 remain OPEN and untouched; SOFTWARE-class vertical proof only (deterministic simulation battery — no physical claim is made or implied)"

standing_rules:
  w048: "accepted-not-restored; MUST NOT be restored implicitly or explicitly"
  architecture: "Architecture 1.1 remains the sole forward design source; no architecture or protocol semantic change is made by this decision"
  history: "No work-item history is rewritten; the M011 ledger entry is appended, never converted"
