# cache-key bump: the incremental build-cache restore chain poisoned the function bundle
# (the zero-config detection failed under restored caches; this comment forces a fresh
# dependency install layer — see docs/deployment/runbook.md §6.4/§6.5 and the fallback
# delivery record)
pg8000==1.31.2
